# ITSM 시스템 구축 방향성 계획서 v3

> `plan.md` → v2에 이어, **그룹사 멀티테넌트 구조 + 계정관리(자체가입/SSO 병행) + 관리자 권한 체계**를 반영한 개정판입니다. AI 챗봇/에이전트 기능은 이번 단계 범위에서 계속 제외합니다.
>
> 최초 가정했던 항목들(테넌트 판별, 데이터 격리, 자체가입 승인 절차 등)은 피드백을 반영해 **확정 처리**했습니다. 남은 미결정 사항은 §9 참고.

## v2 → v3 주요 변경사항

- 아키텍처를 **멀티테넌트 온프레미스**로 전환 — 그룹사 계열사마다 독립된 공간(데이터/설정)으로 사용, 서버는 물리적으로 하나(또는 소수)의 온프레미스 인프라 공유
- 계정관리: **SSO(birtyworks 단일) + 이메일 인증 기반 자체 회원가입**을 함께 지원, 자체가입은 승인 절차 없이 이메일 인증만으로 즉시 활성화
- 관리자 권한을 2단계로 세분화: **통합그룹관리자**(전사 총괄) / **사별관리자**(계열사별 운영)
- 기존 Phase 1 기능(§6)의 "관리자" 표현을 테넌트 구조에 맞게 "사별관리자" 기준으로 명확화
- **테넌트 판별 방식 확정**: 전 계열사가 동일 이메일 도메인(`@wonik.com`)을 사용해 도메인 기준 자동 판별이 불가함을 확인 → 1차는 가입 시 사용자가 계열사를 직접 선택, 이후 birtyworks 연동 스펙 확인되면 자동 판별로 전환하는 단계적 접근으로 변경
- **데이터 격리 방식을 tenant_id 기반으로 확정**하고 동작 원리를 §2-2에 상세 설명 추가

## 1. 한 줄 방향 정의

**그룹사 계열사들이 각자 독립된 공간으로 사용할 수 있는 온프레미스 멀티테넌트 ITSM 도구.** ITIL 핵심 프로세스만 담아 빠르게 도입하되, 국내 그룹웨어/사내 SSO와 매끄럽게 연동되고, 계열사별 독립 운영 + 그룹 통합관리가 동시에 가능한 구조로 설계한다. 추후 IT 외 부서(HR/총무/기획팀 등)로도 확장 가능해야 한다.

## 2. 아키텍처 방향: 멀티테넌트 온프레미스

### 2-1. 테넌트 개념

- **테넌트 = 계열사 단위.** 각 계열사는 자기 데이터(사용자, 티켓, 카탈로그, 자산, KB 등)만 보고 다루며, 다른 계열사의 데이터는 볼 수 없음 //각 계열사만 가능한 항목이 있으며, 그룹 전체가 접근하고 사용할수있는 항목도 있음을 기억해야함
- **배포는 물리적으로 하나(또는 소수)의 온프레미스 서버/인스턴스**에서 이루어지고, 그 안에서 계열사별로 데이터·설정을 논리적으로 분리 — 계열사마다 서버를 따로 세우는 방식이 아님 (관리 부담을 줄이기 위함)

### 2-2. 데이터 격리 전략 (확정: 공유 DB + tenant_id 구분 방식)

**동작 원리**
1. **모든 데이터에 꼬리표를 붙인다**: 사용자, 티켓, 카탈로그 항목, 자산, KB 문서 등 거의 모든 데이터 레코드에 "이건 A계열사 것", "이건 B계열사 것"이라는 `tenant_id` 값을 하나씩 붙여서 저장한다. 물리적으로는 하나의 DB 안에 모든 계열사 데이터가 함께 저장된다.
2. **로그인한 사용자는 자기 tenant_id를 달고 다닌다**: A계열사 직원이 로그인하면 시스템은 "이 사람은 tenant_id=A"라는 것을 세션에 기억해둔다.
3. **모든 조회/저장에 자동으로 필터가 걸린다**: A계열사 직원이 "내 티켓 목록"을 조회하면, 시스템은 항상 `tenant_id = A`인 데이터만 걸러서 보여준다. B계열사의 데이터는 애초에 조회 대상에서 제외되어 화면에 나타나지 않는다.
4. **필터링은 개발자 재량이 아니라 강제되는 공통 장치를 통해야 한다**: 이 필터링을 코드 짤 때마다 개발자가 하나하나 손으로 넣으면, 한 곳이라도 빠뜨리는 순간 다른 계열사 데이터가 새어나오는 사고로 이어진다. 그래서 **모든 데이터 접근이 반드시 거쳐가는 공통 관문(데이터 접근 계층)**을 두고, 그 관문이 tenant_id 필터링을 자동으로 강제하도록 설계한다 — 즉 개발자가 실수로 빠뜨릴 수 없는 구조로 만드는 것이 핵심 원칙.
5. **통합그룹관리자는 예외**: 통합그룹관리자만 관리 화면에서 "지금 어느 계열사를 보고 있는지" 전환할 수 있어 여러 tenant_id를 넘나들며 조회 가능. 일반 사용자·사별관리자는 자기 tenant_id 밖으로 나갈 수 없음.

정리하면, **물리적으로는 한 곳에 섞여 저장되지만, tenant_id라는 꼬리표와 그걸 강제하는 공통 필터링 장치 덕분에 논리적으로는 완전히 분리된 것처럼 동작**한다.

| 방식 | 설명 | 채택 여부 |
|---|---|---|
| 공유 DB + tenant_id 컬럼 구분 | 위 방식 | **확정** — 온프레미스 환경이라 물리적 보안은 이미 확보되어 있고, 관리 단순화에 유리 |
| 계열사별 완전히 별도 DB | 계열사마다 DB를 통째로 분리 | 채택 안 함 |

> ⚠️ 공유 DB 방식을 택했으므로 **모든 데이터 조회/저장 로직에 tenant_id 필터링이 빠짐없이 적용되어야** 다른 계열사 데이터 노출 사고를 막을 수 있습니다. 이건 상세설계 단계에서 반드시 원칙으로 못박아야 하는 부분입니다.

### 2-3. 테넌트별로 분리되어야 하는 것 / 그룹 공통으로 둘 수 있는 것

| 구분 | 계열사별로 독립 | 그룹 공통 (통합그룹관리자가 관리) |
|---|---|---|
| 사용자·조직도 | ✅ 계열사 소속 사용자만 | — | //차후에는 특정 대상자에게 권한 부여시 전사 조직도를 볼 수있도록 하는 기능도 필요함
| 서비스 카탈로그 항목 | ✅ 계열사 자율 항목 추가 가능 | 그룹 공통 템플릿을 배포하면 각 계열사가 그대로 쓰거나 커스터마이징 (가정 — §9에서 범위 재확인 필요) |
| 인시던트 카테고리/담당팀 매핑 | ✅ | — |
| 결재선/합의자 설정 | ✅ | — |
| 자산 목록 | ✅ | — |
| 지식베이스(KB) | ✅ 계열사별 문서 | 공통 KB(예: 전사 보안정책 안내)는 그룹관리자가 전체 배포 가능 |
| SSO/그룹웨어 연동 정보 | ✅ 계열사마다 다른 그룹웨어를 쓸 수 있으므로 계열사별 설정값으로 관리 | — |
| 플랫폼 기능/메뉴 활성화 여부 | — | ✅ 통합그룹관리자가 계열사별로 어떤 메뉴/기능을 켤지 결정 |

### 2-4. 테넌트 판별 방식 (확정: 이메일 도메인 방식 불가 — 단계적 접근으로 변경)

**모든 계열사가 동일한 이메일 도메인(`@wonik.com`)을 사용하므로 도메인 기준 자동 판별은 불가능함을 확인.** 인사정보를 미리 일괄 등록해두는 것도 관리 부담이 커서 현실적이지 않아, 아래처럼 단계적으로 접근한다.

| 단계 | 방식 | 적용 대상 | 비고 |
|---|---|---|---|
| **1차 (지금 바로 구현)** | **자체 회원가입 시 사용자가 소속 계열사를 드롭다운에서 직접 선택** | 자체 회원가입 사용자 | birtyworks API 확인을 기다리지 않고 바로 구현 가능. 인사정보 일괄등록/외부 API 의존 없음. 오선택 가능성은 사별관리자가 신규 가입자 목록에서 소속을 확인/정정할 수 있는 기능으로 방어 |
| **2차 (birtyworks 연동 스펙 확인 후 전환)** | SSO 로그인 시 **birtyworks가 제공하는 회사코드/사업장코드 클레임**으로 자동 판별 | SSO 로그인 사용자 | birtyworks는 그룹 전체 인사/조직 정보를 갖고 있어, 로그인 인증 결과에 회사코드 같은 부가정보(클레임)가 포함되어 있을 가능성이 높음 — 확인되면 SSO 사용자는 이걸로 완전 자동화 |
| **2차 (선택)** | 자체가입 시에도 **birtyworks의 사용자 조회 API로 이메일 매칭**해 자동 판별 | 자체 회원가입 사용자 | birtyworks가 "이메일로 소속 계열사 조회" API를 제공하면, 드롭다운 선택 없이도 자동 배정 가능 |

**즉, 1차는 자체가입 시 사용자가 직접 선택하는 방식으로 지금 바로 진행하고, birtyworks 연동 스펙 확인 결과(§9)에 따라 SSO/자체가입 모두 자동 판별로 전환하는 것을 목표로 한다.** 처음부터 API 의존적으로 설계하면 birtyworks 쪽 확인이 늦어질 때 개발이 막히므로, 폴백(1차) 없이 API만 바라보고 가지 않는다.

## 3. 계정관리 및 인증

### 3-1. 로그인 방식 (SSO + 자체 회원가입 병행)

- **SSO 연동**: 전 계열사가 **birtyworks** 그룹웨어 계정으로 로그인 (확정 — 계열사별로 다른 그룹웨어를 쓰는 곳은 없음)
- **자체 회원가입 (이메일 인증)**: SSO 연동 전이거나 그룹웨어 계정이 없는 사용자를 위해 병행 지원
  1. 이름/사내 이메일/비밀번호로 가입 신청
  2. **소속 계열사를 드롭다운에서 직접 선택** (§2-4 — 도메인이 전 계열사 동일해 자동 판별 불가하므로 1차는 사용자가 직접 선택)
  3. 입력한 이메일로 인증 메일 발송 (인증 링크 또는 인증 코드)
  4. 이메일 인증 완료 시 **계정 즉시 활성화** (별도 승인 절차 없음, §3-2)
- 계열사별로 "SSO만 허용" / "자체가입만 허용" / "둘 다 허용" 중 선택할 수 있게 설정 가능하게 설계 (계열사마다 SSO 연동 준비 상태가 다를 수 있으므로)

### 3-2. 가입 후 권한 부여 (확정: 승인 절차 없이 즉시 활성화)

- 자체 회원가입으로 들어온 신규 계정은 **이메일 인증만으로 즉시 활성화**, 별도의 사별관리자 승인 절차는 두지 않음
- 활성화 시 기본적으로 "일반 사용자" 권한으로 생성되며, 결재자/사별관리자 등 별도 역할은 이후 사별관리자가 필요에 따라 부여
- 다만 잘못된 계열사를 선택해 가입한 경우에 대비해, 사별관리자가 신규 가입자 목록에서 소속 계열사를 확인/정정할 수 있는 기능은 열어둔다

## 4. 관리자 권한 체계 (2단계)

| 레벨 | 역할 | 권한 범위 |
|---|---|---|
| **통합그룹관리자** | 그룹 전체 총괄 | - 계열사(테넌트) 신규 추가/삭제<br>- 각 계열사의 사별관리자 지정/해제<br>- 전체 메뉴·기능 활성화 관리 (계열사별로 어떤 모듈을 켤지)<br>- 그룹 공통 카탈로그 템플릿/공통 정책 배포<br>- 전체 계열사 통합 리포팅 열람 (선택) |
| **사별관리자** | 계열사별 운영 담당 | - 자기 계열사 사용자 계정 관리 (역할 부여/비활성화/소속 오선택 정정)<br>- 자기 계열사 서비스 카탈로그 항목 추가/삭제 (§6-2)<br>- 자기 계열사 인시던트 카테고리·담당팀 매핑 관리<br>- 자기 계열사 결재선/합의자 기본값 지정 (§6-2)<br>- 자기 계열사 리포팅 열람 | //리포팅이라는게 현재 진행중이거나 완료된 티켓을 볼 수 있는 화면인거지? 내용도 볼 수 있고
| **결재자/합의자** | 승인 프로세스 참여자 | 담당 요청 건에 대한 승인/반려 (§6-2) |
| **일반 사용자** | 전 직원 | 티켓 생성/조회, 카탈로그 요청, KB 검색 |

**권한 관계**: 통합그룹관리자는 모든 계열사를 관리 화면에서 전환하며 볼 수 있지만, 사별관리자는 자기 소속 계열사 범위 밖의 데이터는 접근할 수 없음 (§2-2 데이터 격리와 직결).

> 이전 버전(v2)에서 "관리자(담당자)가 카탈로그 항목 추가/삭제" 등으로 표현했던 부분은 이제 **"사별관리자"**로 명확히 합니다.

## 5. 스코프 정의

| 구분 | 내용 |
|---|---|
| 1차 스코프 (MVP) | 멀티테넌트 기반 위에서 — 인시던트, 서비스 요청, 자산관리 기본, 지식베이스, 이메일 자동 티켓화(POP3 폴링), 2단계 관리자 권한 |
| 확장 여지 (설계는 지금부터 고려) | 문제관리, 변경관리, SLA 고도화, 타 부서(HR/총무/기획팀 등 ESM) 확장, 그룹 통합 리포팅 고도화 |
| 아직 안 함 (later) | AI 챗봇/자동화, 고급 리포팅/BI, 모바일 전용 앱 |
| 규모 가정 | 계열사 수 및 계열사별 사용자 규모 미정 — 구조 변경 없이 계열사 추가 가능해야 함 |

## 6. Phase 1 (MVP) 핵심 기능

### 6-1. 서비스 데스크 / 인시던트 관리

- **티켓 생성 경로**: 웹 포털이 기본 경로. 여기에 더해 **사내 POP3/SMTP 인프라를 활용한 이메일 자동 티켓화도 1차 스코프로 확정** — 상세 방식은 §6-1-1 참고 (계열사별로 전용 메일 계정을 따로 둘지, 공용 계정 하나로 받고 발신 도메인으로 테넌트를 구분할지는 §9에서 확인 필요)
- **진입 구조 (인시던트 vs 서비스요청 명확히 구분)**:
  - 인시던트(장애 신고)는 **단일 신고 창구** 하나로 운영 — 포털에 "장애 신고" 버튼 하나, 폼 안에서 카테고리(예: 네트워크/계정/하드웨어)를 선택하면 해당 계열사 내 카테고리 담당팀에게 자동 배정
  - 서비스 요청은 **카탈로그 항목별로 진입점(폼) 자체가 별도** — "팀/카테고리 라우팅" 개념이 아니라 항목 선택 자체가 곧 담당 파악 (§6-2 참고)
- 상태 흐름: 접수 → 처리중 → 대기(사용자 확인 필요) → 해결 → 종료 (커스텀 가능하게)
- 담당자 배정: 인시던트는 카테고리 기반 자동 1차 배정 + 필요 시 수동 재배정 (모두 소속 계열사 범위 내에서만)
- **댓글/내부 메모**: 댓글 작성 시 기본값은 사용자에게 보이는 일반 답변. "내부 메모" 체크박스를 켜면 담당자에게만 보이는 메모로 전환
- 첨부파일, 스크린샷 첨부
- **우선순위/SLA 타이머**: 우선순위(예: 긴급/보통/낮음)별로 "몇 시간 내 첫 응답, 몇 시간 내 해결"이라는 목표 시간을 설정해두고 시스템이 카운트다운. 기한을 넘기면 경고/에스컬레이션 알림 발생. MVP는 3단계 우선순위 + 단순 응답시간 기준으로 가볍게 시작

### 6-1-1. 이메일 자동 티켓화 (POP3 폴링 방식, 확정)

사내에 **POP3(수신)/SMTP(발신)**가 제공됨을 확인 — 별도 메일 서버나 외부 서비스 없이 기존 인프라로 구현 가능하다고 판단해 1차 스코프에 포함한다.

| 방식 | 설명 | 장단점 |
|---|---|---|
| 기존 메일함 폴링 (POP3) | `it-help@company.com` 같은 전용 계정을 만들고, ITSM 서버가 주기적으로 그 메일함을 POP3로 조회해서 새 메일을 읽어옴 | 별도 인프라 불필요, 기존 사내 메일 시스템(birtyworks 등) 그대로 활용. 다만 폴링 주기만큼 지연 발생, 계정 자격증명을 서버에 저장해야 함 |

**기본 동작 방식**
1. IT지원용 전용 메일 계정(예: `it-help@company.com`)을 하나 생성
2. ITSM 서버가 일정 주기(예: 1~5분)로 해당 계정을 POP3로 폴링해 새 메일 확인
3. 제목 → 티켓 제목, 본문 → 티켓 내용, 발신자 → 요청자로 자동 매핑해 티켓 생성
4. 알림/답장 등 발신 메일은 SMTP로 처리 (§6-6 알림과 연동)

**POP3 특성상 추가로 고려해야 할 점** (IMAP과 달리 서버 쪽에서 읽음 상태/폴더 관리를 지원하지 않음)
- **중복 처리 방지**: 폴링할 때마다 전체 메일함을 다시 읽지 않도록, 이미 처리한 메일을 시스템이 자체적으로 추적해야 함 (예: 메일의 고유 식별자를 저장해두고 비교)
- **서버 보관 정책**: 폴링 후 메일을 서버에서 삭제할지, 남겨둘지 결정 필요 — 삭제하면 유실 위험(장애 시 복구 불가), 남겨두면 사내 메일함 용량이 계속 쌓임 → 일정 기간 후 정리하는 정책 필요
- **스레드 매칭**: 사용자가 알림 메일에 회신하면 새 티켓이 아니라 기존 티켓에 댓글로 붙어야 함 (제목의 티켓번호 또는 메일 헤더로 매칭)
- **스팸/자동회신 루프 방지**: 시스템이 보낸 알림 메일에 부재중 자동응답 등이 회신하면서 무한 루프가 도는 것을 방지하는 로직 필요
- **자격증명 관리**: 전용 계정의 POP3 로그인 정보를 ITSM 서버에 안전하게 저장(환경변수/시크릿 매니저 등)
- **멀티테넌트 고려**: 계열사마다 메일 계정을 따로 둘지, 발신 주소로 테넌트를 구분할지 결정 필요 (§9)

### 6-2. 서비스 카탈로그 (요청 관리)

- 표준화된 요청 항목 정의 (예: "노트북 신청", "계정 권한 변경", "VPN 발급")
- **항목은 사별관리자가 관리 화면에서 자유롭게 추가/삭제** 가능 (그룹 공통 템플릿이 있다면 통합그룹관리자가 배포, §2-3)
- **승인 워크플로우 (다단계 + 결재선/합의자 구조로 확정)**:
  1. 요청자가 카탈로그 항목을 요청
  2. 접수자(담당자)가 요청을 접수하고, 건별로 **결재선**과 **합의자**를 지정 (사별관리자 페이지에서 항목별 기본 결재선/기본 합의자를 미리 설정해두는 것도 가능)
  3. **결재 단계** — 아래 두 그룹이 동시에 진행됨
     - **결재선 (순차/직렬)**: 결재자를 여러 명 지정 가능하며, 지정된 순서대로 1차 결재자 승인 → 2차 결재자 승인 → ... 순으로 진행
     - **합의자 (병렬/동시)**: 결재선이 진행되는 동안 **동시에** 검토하며, 결재자와 **동일한 승인/반려 권한**을 가짐
     - 결재선의 결재자 중 한 명이라도 반려하거나, 합의자 중 한 명이라도 반려하면 **전체 요청이 반려 처리**됨
     - 결재선 전원의 순차 승인 + 합의자 전원의 승인이 모두 완료되어야 최종 승인
  4. 승인되면 접수자(담당자)가 실제 업무를 처리
- 승인/반려 시 알림 (결재선의 각 순번, 합의자 각각에게 개별적으로 승인 요청 알림 발송)
- 결재자/합의자는 **같은 계열사 소속 인원 중에서만** 지정 가능 (계열사 간 결재 교차는 1차 스코프에서 지원하지 않음)
- //여기서 UI 같은게 궁금해, 접수자가 결재자 등등의 라인이 ui로써 어디까지 진행중인지 표시가 되는지 어떻게 표시되는지,그리고 승인후 담당자가 실제 업무 접수되면 최초 요청자와 담당자간 세부적으로 메세지를 주고받을때 댓글 형식으로 되는지 아니면 다른 좋은 방향이 있는지

### 6-3. 자산관리 (간단한 수준)

- IT 자산 등록(하드웨어/소프트웨어 라이선스) — CMDB까지는 안 가고 대장 수준
- 자산-사용자 매핑 (누가 어떤 장비를 쓰는지), 계열사별로 자산 목록 분리
- 티켓과 자산 연결(어떤 장비에서 발생한 인시던트인지)

### 6-4. 지식베이스 / FAQ

- 카테고리별 문서 작성/검색 (계열사별 KB + 그룹 공통 KB 병행, §2-3)
- 티켓 해결 시 관련 KB 문서 연결 → 반복 문의 감소

### 6-5. 셀프서비스 포털

- 사용자가 직접 티켓 조회/생성, 카탈로그 요청, KB 검색
- 내 요청 현황 대시보드
- 로그인 시 자신이 속한 계열사 화면으로 자동 진입 (테넌트 전환 없음 — 일반 사용자는 자기 계열사만 봄)
// 요청자가 내 요청현황이 있는 것 처럼  접수자 담당자도 이런 요청 리스트를 볼 수 있는 화면이 있는지 그리고 반대로 요청자도 다른 영역에서는 담당자가 될 수 도 있는데
이걸 어떤식으로 구조를 잡고갈지 확인 필요 UI면에서도 어떤식으로 
### 6-6. 알림

- 이메일 알림 기본 지원
- **사내 그룹웨어 메신저 연동이 가능하면 연동 예정** (티켓 생성/상태변경/댓글/승인요청 시)
- 담당자 SLA 임박 알림

### 6-7. 기본 리포팅

- 계열사별 티켓 처리 건수, 평균 처리시간, 담당자별 처리량 대시보드 (사별관리자용)
- 전체 계열사 통합 대시보드 (통합그룹관리자용, 선택)

## 7. 국내 환경 특화 요구사항 (차별화 포인트)

| 항목 | 내용 |
|---|---|
| SSO 연동 | **전 계열사가 birtyworks 그룹웨어를 공통으로 사용 (확정).** **OIDC(OpenID Connect)** 방식으로 연동 가능함을 확인함. 계열사별로 다른 그룹웨어를 쓰는 곳은 없어 인증 연동 자체는 단일화 가능 (§3-1) |
| 자체 회원가입 | SSO 연동 전이거나 그룹웨어 계정이 없는 사용자를 위한 이메일 인증 기반 가입 병행, **이메일 인증만으로 즉시 활성화** (§3-1~3-2) |
| 사내 메신저 연동 | 사내 그룹웨어 메신저 연동이 가능하면 알림/티켓 생성 채널로 연동 예정 |
| 그룹웨어 연동 | birtyworks와 조직도/사용자 정보 동기화 — 조직 개편 시 사용자/부서 정보 자동 반영 |
| 조직도 기반 권한 | 부서/직급 기반 승인 라인 반영 → 승인자 지정을 "조직도 상 상위자"로 자동 매핑하는 기능 고려 |
| 한국어 UX | 존댓말 톤, 날짜/시간 형식, 공휴일 캘린더(SLA 계산 시 한국 공휴일 반영) |
| 인증/보안 규제 | 개인정보보호법, 접근이력 로그 요건 등 국내 컴플라이언스 **반드시 고려** — 계열사 간 데이터 격리도 이 컴플라이언스의 일부 |

## 8. 확장을 고려한 설계 방향

1. 티켓/카테고리 모델을 IT 전용으로 하드코딩하지 않기 — **HR/총무/기획팀 등에서도 향후 사용 가능하도록** "서비스 유형"을 부서별로 확장 가능한 형태로 설계
2. 승인 워크플로우를 범용화 — IT 승인이든 총무 승인이든 같은 워크플로우 엔진(접수자→결재선/합의자→처리)을 재사용할 수 있게. 결재선/합의자 구조는 향후 법무/재무 등으로 확장 시에도 그대로 재사용 가능해야 함
3. 사용자/조직 정보를 그룹웨어(계열사별)에서 동기화하는 구조를 처음부터 반영 — 수동 사용자 관리 방식은 나중에 뜯어고쳐야 하므로 지양
4. 권한 체계를 역할(Role) 기반으로 — "IT 담당자"뿐 아니라 "HR 담당자", "매니저", "통합그룹관리자" 등 역할도 쉽게 추가되게
5. **신규 계열사(테넌트) 추가가 코드 변경 없이 데이터 등록만으로 가능해야 함** — 계열사가 늘어날 때마다 개발 작업이 필요하면 멀티테넌트 설계의 의미가 없음

## 9. 다음 단계 (미결정 사항)

**이번 개정으로 확정된 것**: 테넌트 판별(1차: 가입 시 직접 선택 / 2차: birtyworks 자동화 전환), 전 계열사 birtyworks 단일 SSO, 데이터 격리(공유 DB+tenant_id), 자체가입 승인 절차 없음(이메일 인증만으로 즉시 활성화).

**여전히 확인이 필요한 항목:**

1. **birtyworks의 정확한 연동 스펙** — 이번 개정으로 특히 중요해진 두 가지를 반드시 포함해서 확인 필요:
   - OIDC 로그인 응답(클레임)에 회사코드/사업장코드 같은 계열사 식별 정보가 포함되는지 (§2-4 방법 A)
   - 이메일로 소속 계열사를 조회할 수 있는 사용자 조회 API를 제공하는지 (§2-4 방법 B)
   - 조직도/계정 동기화용 API 제공 여부
2. **계열사(테넌트) 목록 관리 방식**: 시스템에 계열사 목록을 어떻게 최초 등록하고 이후 추가/변경할지 (통합그룹관리자가 관리 화면에서 직접 등록하는 것으로 진행해도 되는지 확인) // 통합그룹 관리자가 직저 등록해서 테넌트 추가하는 방식으로 사용 
3. 자체 회원가입 드롭다운에 노출할 계열사 목록의 최초 데이터 (계열사명, 코드 등) // 자체 회원가입은 보류 일단 메일인증할때 계열사정보를 가져올 수 있는지 부터 확인하자 이부분을 좀 도와줘 어떻게하면 확인 가능한지
4. **그룹 공통 카탈로그/KB의 범위**: 통합그룹관리자가 배포하는 공통 항목을 계열사가 수정 가능하게 할지, 강제 적용할지 // 배포한다는게 어떤의미야??
5. **이메일 자동 티켓화의 테넌트 구분 방식**: 계열사별 전용 메일 계정을 각각 둘지, 공용 계정 + 발신 도메인으로 구분할지 //  이거는 전용메일 각자 두는 케이스로 진행하되 나중에 공용계정으로 자동 테넌트별로 등록 되는것도 구현할 수 있게 열어두고 설계해줘
6. 서비스 카탈로그 초기 항목 목록 (계열사별로 처음에 등록할 요청 항목들) // 보통 ITSM에서 자주 사용하는 항목들 몇 가지만 초기로 설정해주고 나머지는 각자 추가할 수 있게
7. 인시던트 카테고리 목록 및 카테고리-담당팀 매핑표 (계열사별로) // 이게 어떤 의미인지 설명좀
8. 결재자/합의자 목록 및 기본값 지정 (조직상 누가 결재선/합의자 역할을 맡는지, 계열사별로) //결재선 기본 값은 일단 없는걸로하고 계열사 관리자가 필요하면 설정 가능하게 해줘
9. IT지원용 전용 메일 계정 발급 요청 (사내 메일 시스템/IT인프라팀에 문의, POP3/SMTP 접속 정보 확보) // 이건 나중에 개발하면서 해보자
10. 이메일 폴링 주기 및 서버 메일 보관 정책 결정 // 폴링이 정확히 어떤건지 설명해주고 이것도 나중에 개발하면서 맞춰보자

## 10. 이번 단계 범위 제외 항목

- AI 챗봇/자동 응답, AI 기반 티켓 자동 분류·처리 등 AI/에이전트 기능 전반 — 추후 별도 검토
- 계열사 간 결재 교차(한 계열사 요청을 다른 계열사 인원이 결재) — 1차 스코프에서는 지원하지 않음
