# ITSM 시스템 상세 기능요구서 및 화면설계

> 이 문서는 `itsm_plan2.md`(방향성 계획서 v3)를 기반으로 작성한 상세 기능요구서입니다. AI 챗봇/에이전트 기능은 범위에서 제외합니다.
> 화면설계는 실제 디자인/코드가 아닌, 화면 구성요소와 레이아웃을 정의하는 텍스트 기반 와이어프레임입니다.

## 0. 문서 표기 규칙

- **기능요구사항 ID**: `FR-[모듈약어]-[번호]` (예: `FR-AUTH-001`)
- **화면 ID**: `SC-[모듈약어]-[번호]` (예: `SC-AUTH-01`)
- **사용자 유형**: 일반사용자 / 결재자·합의자 / 사별관리자 / 통합그룹관리자
- **모듈 약어**: AUTH(인증) · INC(인시던트) · MAIL(이메일티켓화) · CAT(카탈로그) · AST(자산) · KB(지식베이스) · PORT(포털/대시보드) · NOTI(알림) · RPT(리포팅) · ADM(관리자) · TEN(테넌트)

---

## 1. 전체 정보구조(IA)

```
로그인 (SC-AUTH-01)
 ├─ [일반사용자] 개인 대시보드 (SC-PORT-01)
 │    ├─ 내가 요청한 것 → 티켓/카탈로그 요청 상세 (SC-INC-03 / SC-CAT-03)
 │    ├─ 내가 처리할 것 (담당자 배정 시에만 노출)
 │    ├─ 내가 결재·합의할 것 (결재자/합의자 지정 시에만 노출)
 │    ├─ 장애 신고 (SC-INC-01)
 │    ├─ 서비스 카탈로그 (SC-CAT-01 → SC-CAT-02)
 │    └─ 지식베이스 검색 (SC-KB-01)
 │
 ├─ [사별관리자] 관리자 콘솔 (자기 계열사 범위)
 │    ├─ 전체 티켓 리스트 (SC-RPT-02)
 │    ├─ 리포팅 대시보드 (SC-RPT-01)
 │    ├─ 사용자 관리 (SC-ADM-01)
 │    ├─ 서비스 카탈로그 관리 (SC-ADM-02)
 │    ├─ 인시던트 카테고리 관리 (SC-ADM-03)
 │    └─ 결재선/합의자 기본값 설정 (SC-ADM-04)
 │
 └─ [통합그룹관리자] 그룹 관리 콘솔 (전 계열사 범위 + 계열사 전환)
      ├─ 계열사(테넌트) 관리 (SC-TEN-01)
      ├─ 사별관리자 지정 (SC-TEN-02)
      ├─ 전 계열사 통합 티켓 리스트 (SC-RPT-02, 계열사 필터 포함)
      ├─ 그룹 공통 카탈로그/KB 배포 (SC-ADM-02 확장)
      └─ 메뉴·기능 활성화 관리 (SC-TEN-03)
```

---

## 2. 공통 요구사항

| ID | 요구사항 |
|---|---|
| FR-COM-001 | 모든 데이터 조회/저장은 로그인 사용자의 `tenant_id` 기준으로 자동 필터링되어야 하며, 이 필터링은 개별 화면/기능 단위가 아니라 공통 데이터 접근 계층에서 강제되어야 한다 (`itsm_plan2.md` §2-2) |
| FR-COM-002 | 통합그룹관리자는 화면 상단에서 "조회 중인 계열사"를 전환할 수 있는 선택 UI를 가진다. 미선택 시 기본은 "전체" |
| FR-COM-003 | 모든 화면은 로그인 세션 만료 시 로그인 화면으로 리다이렉트하고, 원래 접근하려던 화면으로 로그인 후 복귀한다 |
| FR-COM-004 | 헤더 영역은 전 화면 공통으로 로고, 알림 아이콘(읽지 않은 알림 수 뱃지), 사용자 메뉴(이름, 소속 계열사, 로그아웃)를 포함한다 |
| FR-COM-005 | 목록형 화면(티켓 목록, 자산 목록, KB 목록 등)은 공통적으로 검색·필터·페이지네이션을 제공한다 |

---

## 3. 인증/계정관리 (AUTH)

### 3-1. 기능요구사항

| ID | 요구사항 | 비고 |
|---|---|---|
| FR-AUTH-001 | SSO(birtyworks) 로그인 버튼 제공, 클릭 시 birtyworks 인증 화면으로 리다이렉트 | |
| FR-AUTH-002 | SSO 인증 성공 후 반환된 이메일로 사내 인사DB를 조회해 소속 계열사(tenant_id)를 자동 판별한다 | `itsm_plan2.md` §2-4 |
| FR-AUTH-003 | 인사DB에 없는 이메일로 SSO 인증이 성공한 경우, 로그인을 차단하고 관리자 문의 안내 메시지를 표시한다 | 예외 케이스 |
| FR-AUTH-004 | 자체 회원가입 폼은 이름/사내 이메일/비밀번호를 입력받는다 | |
| FR-AUTH-005 | 가입 신청 제출 시 인증메일 발송 **전에** 입력 이메일로 인사DB를 조회한다 | |
| FR-AUTH-006 | 인사DB 조회 성공 시 조회된 소속 계열사를 내부적으로 임시 저장하고 인증메일을 발송한다 | |
| FR-AUTH-007 | 인사DB 조회 실패 시(신규입사자 등) 소속 계열사를 사용자가 직접 선택하는 드롭다운을 노출한다 | 폴백 |
| FR-AUTH-008 | 인증메일의 링크(또는 코드) 확인 시 계정을 즉시 활성화하고, FR-AUTH-006/007에서 확보한 계열사로 tenant_id를 배정한다. 별도 승인 절차 없음 | |
| FR-AUTH-009 | 인증메일은 24시간 내 미인증 시 만료되며, 재발송 기능을 제공한다 | |
| FR-AUTH-010 | 신규 계정은 기본적으로 "일반 사용자" 역할로 생성된다 | |
| FR-AUTH-011 | 이메일 조회 시 대소문자/공백을 정규화한 뒤 비교한다 | |
| FR-AUTH-012 | 계열사별로 로그인 방식을 "SSO만" / "자체가입만" / "둘 다"로 설정할 수 있다 (통합그룹관리자 또는 사별관리자 설정) | |

### 3-2. 화면설계

**SC-AUTH-01. 로그인 화면**

```
┌───────────────────────────────────┐
│              [로고]                 │
│                                     │
│      [ birtyworks로 로그인 ]        │
│                                     │
│    ────────── 또는 ──────────      │
│                                     │
│   이메일    [______________]       │
│   비밀번호  [______________]       │
│              [ 로그인 ]             │
│                                     │
│   계정이 없으신가요? [회원가입]      │
└───────────────────────────────────┘
```
| 구성요소 | 설명 |
|---|---|
| birtyworks로 로그인 버튼 | SSO 진입점 |
| 이메일/비밀번호 폼 | 자체가입 계정 로그인용 (계열사 설정에 따라 숨김 가능, FR-AUTH-012) |

**SC-AUTH-02. 회원가입 화면**

```
┌───────────────────────────────────┐
│  이름       [______________]       │
│  사내 이메일 [______________]       │
│  비밀번호   [______________]       │
│  비밀번호 확인 [______________]     │
│                                     │
│  (이메일 조회 실패 시에만 노출)       │
│  소속 계열사 [ 선택하세요      ▼]   │
│                                     │
│           [ 가입 신청 ]             │
└───────────────────────────────────┘
```

**SC-AUTH-03. 이메일 인증 대기 화면**
- "입력하신 이메일로 인증 메일을 발송했습니다" 안내 + 재발송 버튼 + 남은 유효시간 표시

---

## 4. 인시던트 관리 / 서비스데스크 (INC)

### 4-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-INC-001 | 인시던트(장애신고)는 단일 신고 폼 하나로 접수하며, 폼 내 카테고리 드롭다운(계열사별로 관리자가 설정, §ADM-03)을 필수로 선택한다 |
| FR-INC-002 | 카테고리 선택 시 사별관리자가 미리 설정해둔 카테고리-담당팀 매핑에 따라 담당자/담당팀에 자동 1차 배정한다 |
| FR-INC-003 | 담당자는 배정된 티켓을 다른 담당자에게 수동 재배정할 수 있다 (같은 계열사 소속 인원만) |
| FR-INC-004 | 티켓 상태는 접수 → 처리중 → 대기(사용자 확인 필요) → 해결 → 종료 순으로 전이하며, 사별관리자가 상태값을 커스터마이징할 수 있다 |
| FR-INC-005 | 티켓 상세 화면에서 댓글을 작성할 수 있으며, "내부 메모" 체크박스 선택 시 담당자에게만 노출된다 |
| FR-INC-006 | 티켓에 첨부파일(이미지 포함)을 첨부할 수 있다 |
| FR-INC-007 | 우선순위(긴급/보통/낮음)를 선택하면 사전 정의된 SLA 목표시간(응답/해결)이 자동 설정되고 카운트다운이 시작된다 |
| FR-INC-008 | SLA 목표시간 초과 시 담당자와 사별관리자에게 에스컬레이션 알림을 발송한다 |
| FR-INC-009 | 티켓은 요청자·담당자·같은 계열사 소속 사별관리자/통합그룹관리자만 조회 가능하다 (tenant_id 격리) |

### 4-2. 화면설계

**SC-INC-01. 장애 신고 폼**

```
┌───────────────────────────────────────┐
│  장애 신고                              │
│  제목      [___________________]       │
│  카테고리  [ 네트워크        ▼]         │
│  우선순위  ( )긴급 (●)보통 ( )낮음      │
│  내용      [                     ]      │
│            [                     ]      │
│  첨부파일  [ 파일 선택 ]                │
│                    [ 취소 ] [ 제출 ]    │
└───────────────────────────────────────┘
```

**SC-INC-02. 티켓 상세 화면**

```
┌───────────────────────────────────────────┐
│ #1023 VPN 접속이 안 됩니다        [처리중]  │
│ 요청자: 홍길동   담당자: 김담당   우선순위:보통│
│ SLA: 해결까지 3시간 12분 남음               │
├───────────────────────────────────────────┤
│ [내용]                                      │
│ VPN 접속 시도 시 인증 오류가 발생합니다...    │
│ 📎 스크린샷.png                             │
├───────────────────────────────────────────┤
│ [댓글]                                      │
│  홍길동: 재부팅해도 동일합니다               │
│  김담당(내부메모🔒): 계정 잠금 확인 필요      │
│                                              │
│ [ 답변 작성________________] □내부메모 [등록]│
├───────────────────────────────────────────┤
│ 상태 변경: [처리중 ▼]  담당자 재배정: [▼]    │
└───────────────────────────────────────────┘
```

**SC-INC-03. 내 요청 상세** — SC-INC-02와 동일 레이아웃, 상태변경/재배정 컨트롤만 숨김(요청자는 조회·댓글만 가능)

---

## 5. 이메일 자동 티켓화 (MAIL)

### 5-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-MAIL-001 | 계열사별 전용 메일 계정(POP3)을 시스템에 등록할 수 있으며, 각 계정에는 tenant_id가 매핑된다 |
| FR-MAIL-002 | 등록된 각 계정을 설정된 주기(기본값 제안: 5분)로 POP3 폴링하여 신규 메일을 확인한다 |
| FR-MAIL-003 | 신규 메일 수신 시 제목→티켓 제목, 본문→내용, 발신자→요청자로 매핑해 인시던트 티켓을 자동 생성한다 |
| FR-MAIL-004 | 처리한 메일의 고유 식별자를 저장해 중복 처리를 방지한다 |
| FR-MAIL-005 | 회신 메일은 제목의 티켓번호 또는 메일 헤더로 기존 티켓을 찾아 댓글로 추가한다 (신규 티켓 생성 금지) |
| FR-MAIL-006 | 시스템이 발송한 알림 메일에 대한 자동회신(부재중 등)은 티켓/댓글로 처리하지 않도록 발신 주소 필터링을 적용한다 |
| FR-MAIL-007 | 폴링 후 메일 서버 보관 정책(삭제/유지 기간)을 설정할 수 있다 — 상세값은 개발 단계에서 결정 |
| FR-MAIL-008 | 계정 자격증명은 암호화하여 저장한다 |
| FR-MAIL-009 | (확장 여지) 공용 계정 + 발신자 기반 자동 테넌트 판별로 전환 가능하도록, 파싱 로직은 "계정 기준"과 "발신자 기준" 두 방식을 모두 지원할 수 있는 구조로 설계한다 |

### 5-2. 화면설계

**SC-ADM-05. 이메일 계정 설정 (사별관리자)**

| 항목 | 설명 |
|---|---|
| 계정 주소 | `it-help-A@wonik.com` 형태 입력 |
| POP3 서버/포트/자격증명 | 연동 정보 입력 |
| 폴링 주기 | 분 단위 설정 |
| 활성화 여부 | on/off |

---

## 6. 서비스 카탈로그 + 결재 워크플로우 (CAT)

### 6-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-CAT-001 | 사별관리자는 카탈로그 항목(제목/설명/입력 폼 필드)을 추가·수정·삭제할 수 있다 |
| FR-CAT-002 | 통합그룹관리자가 배포한 공통 카탈로그 항목은 각 계열사 카탈로그에 자동 노출된다 |
| FR-CAT-003 | 사용자는 카탈로그 목록에서 항목을 선택해 전용 요청 폼을 작성·제출한다 |
| FR-CAT-004 | 접수자(담당자)는 제출된 요청에 결재선(1인 이상, 순서 지정)과 합의자(0인 이상)를 지정한다. 항목별 기본값은 없다 |
| FR-CAT-005 | 결재선은 지정된 순서대로 순차 승인되며, 이전 순번이 승인해야 다음 순번에게 요청이 넘어간다 |
| FR-CAT-006 | 합의자는 결재선 진행과 동시에 검토하며, 결재자와 동일한 승인/반려 권한을 가진다 |
| FR-CAT-007 | 결재선 또는 합의자 중 한 명이라도 반려하면 전체 요청이 즉시 반려 처리된다 |
| FR-CAT-008 | 결재선 전원 순차 승인 + 합의자 전원 승인이 모두 완료되면 최종 승인 처리되고 접수자에게 처리 시작 알림이 간다 |
| FR-CAT-009 | 결재자/합의자는 같은 계열사 소속 인원 중에서만 지정 가능하다 |
| FR-CAT-010 | 승인이 완료된 요청은 인시던트 티켓과 동일한 댓글/내부메모 구조를 사용해 요청자-담당자가 소통한다 (FR-INC-005 재사용) |
| FR-CAT-011 | 결재/합의 각 단계 전환 시 대상자에게 개별 알림을 발송한다 |

### 6-2. 화면설계

**SC-CAT-01. 카탈로그 목록**

```
┌───────────────────────────────────────┐
│  서비스 카탈로그          검색[______]  │
├───────────────────────────────────────┤
│ [PC/노트북 신청]  [계정 권한 변경]      │
│ [VPN 접속 신청]   [비밀번호 초기화]     │
│ [소프트웨어 설치] [모바일 기기 신청]    │
└───────────────────────────────────────┘
```

**SC-CAT-02. 카탈로그 요청 폼** — 항목별 커스텀 필드 + 제출 버튼 (SC-INC-01과 유사 구조)

**SC-CAT-03. 결재 진행상황 (요청 상세 화면 내)**

```
┌───────────────────────────────────────────┐
│ #REQ-0231 노트북 신청              [결재중] │
├───────────────────────────────────────────┤
│ 결재선 (순차)                               │
│  ① 김팀장 ✅승인  → ② 이본부장 ⏳대기중      │
│                                              │
│ 합의자 (병렬)                               │
│  박보안 ✅승인   최자산관리 ⏳대기중          │
├───────────────────────────────────────────┤
│ [댓글] (승인 완료 후 노출)                   │
└───────────────────────────────────────────┘
```

**SC-CAT-04. 결재 처리 화면 (결재자/합의자용)** — 요청 내용 요약 + [승인] [반려(사유 입력)] 버튼

---

## 7. 자산관리 (AST)

### 7-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-AST-001 | 사별관리자는 IT 자산(하드웨어/소프트웨어 라이선스)을 등록·수정·삭제할 수 있다 |
| FR-AST-002 | 자산은 사용자와 매핑(지급/회수 이력 포함)할 수 있다 |
| FR-AST-003 | 인시던트 티켓 생성/상세 화면에서 관련 자산을 연결할 수 있다 |
| FR-AST-004 | 자산 목록/상세는 tenant_id로 격리된다 |

### 7-2. 화면설계

**SC-AST-01. 자산 목록** — 자산명/유형/시리얼번호/사용자/상태 컬럼의 테이블 + 검색·필터
**SC-AST-02. 자산 상세** — 자산 정보 + 지급 이력 + 연결된 티켓 목록

---

## 8. 지식베이스 (KB)

### 8-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-KB-001 | 카테고리별 문서 작성/수정/삭제 (사별관리자 및 권한 부여된 담당자) |
| FR-KB-002 | 제목/본문 키워드 검색 |
| FR-KB-003 | 티켓 처리 시 관련 KB 문서를 연결해 첨부할 수 있다 |
| FR-KB-004 | 계열사별 KB와 그룹 공통 KB(통합그룹관리자 배포)를 구분해 노출한다 |

### 8-2. 화면설계

**SC-KB-01. KB 목록/검색** — 카테고리 트리 + 검색창 + 문서 리스트
**SC-KB-02. KB 상세/작성** — 제목/카테고리/본문 에디터

---

## 9. 셀프서비스 포털 / 개인 대시보드 (PORT)

### 9-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-PORT-001 | 로그인 시 자신이 속한 계열사 화면으로 자동 진입한다 (테넌트 전환 없음) |
| FR-PORT-002 | 개인 대시보드는 "내가 요청한 것 / 내가 처리할 것 / 내가 결재·합의할 것" 3개 탭으로 구성된다 |
| FR-PORT-003 | "내가 처리할 것" 탭은 담당자로 배정된 건이 없으면 탭 자체를 숨긴다 |
| FR-PORT-004 | "내가 결재·합의할 것" 탭은 결재자/합의자로 지정된 건이 없으면 탭 자체를 숨긴다 |
| FR-PORT-005 | 각 탭 목록에서 항목 클릭 시 해당 상세 화면(SC-INC-02/03, SC-CAT-03/04)으로 이동한다 |

### 9-2. 화면설계

**SC-PORT-01. 개인 대시보드**

```
┌─────────────────────────────────────────────┐
│ [내가 요청한 것] [내가 처리할 것] [결재할 것]  │
├─────────────────────────────────────────────┤
│ 번호   제목              상태     날짜        │
│ #1023  VPN 접속 안됨     처리중   08-10       │
│ #REQ22 노트북 신청       결재중   08-09       │
│ #1019  이메일 문의       완료     08-05       │
└─────────────────────────────────────────────┘
```

---

## 10. 알림 (NOTI)

### 10-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-NOTI-001 | 티켓 생성/상태변경/댓글/승인요청 시 관련자에게 이메일 알림을 발송한다 |
| FR-NOTI-002 | 사내 그룹웨어 메신저 연동이 확인되면 동일 이벤트에 메신저 알림도 발송한다 (확장) |
| FR-NOTI-003 | SLA 임박/초과 시 담당자와 사별관리자에게 알림을 발송한다 |
| FR-NOTI-004 | 시스템 내 알림함(헤더 아이콘)에서 최근 알림을 확인할 수 있다 |

### 10-2. 화면설계

**SC-NOTI-01. 알림함(드롭다운)** — 헤더 알림 아이콘 클릭 시 최근 알림 리스트 노출, 클릭 시 관련 화면 이동

---

## 11. 리포팅 & 티켓 전체 리스트 (RPT)

### 11-1. 기능요구사항

| ID | 요구사항 |
|---|---|
| FR-RPT-001 | 사별관리자는 자기 계열사의 티켓 처리 건수/평균 처리시간/담당자별 처리량 통계를 조회한다 |
| FR-RPT-002 | 통합그룹관리자는 전 계열사 통합 통계를 조회하며, 계열사별로 필터링할 수 있다 |
| FR-RPT-003 | 사별관리자/통합그룹관리자는 담당자 배정 여부와 무관하게 자기 권한 범위 내 전체 티켓을 리스트로 조회할 수 있다 (리포팅과 별개 기능) |
| FR-RPT-004 | 전체 티켓 리스트는 상태/카테고리/담당자/기간으로 필터링 가능하다 |
| FR-RPT-005 | 리스트에서 개별 티켓 클릭 시 상세(댓글, 처리 이력 포함) 조회가 가능하다 |

### 11-2. 화면설계

**SC-RPT-01. 통계 대시보드** — 처리 건수/평균 처리시간/담당자별 처리량 차트 및 요약 카드

**SC-RPT-02. 전체 티켓 리스트**

```
┌─────────────────────────────────────────────────┐
│ 전체 티켓  [계열사: 전체 ▼](그룹관리자만 노출)      │
│ 상태[▼] 카테고리[▼] 담당자[▼] 기간[____~____][검색]│
├─────────────────────────────────────────────────┤
│ 번호  제목        계열사  상태   담당자  요청일     │
│ 1023  VPN 접속... A사    처리중 김담당  08-10      │
│ 1022  노트북...   B사    완료   이담당  08-08      │
└─────────────────────────────────────────────────┘
```

---

## 12. 관리자 기능 (ADM / TEN)

### 12-1. 기능요구사항 — 사별관리자

| ID | 요구사항 |
|---|---|
| FR-ADM-001 | 자기 계열사 사용자 목록 조회, 역할 부여(결재자 등), 계정 비활성화 |
| FR-ADM-002 | 소속 오선택으로 가입한 사용자의 계열사를 정정 (§AUTH-007 폴백 케이스 처리) |
| FR-ADM-003 | 서비스 카탈로그 항목 추가/수정/삭제 (FR-CAT-001) |
| FR-ADM-004 | 인시던트 카테고리 및 카테고리-담당팀 매핑 관리 |
| FR-ADM-005 | 이메일 계정(POP3) 설정 관리 (§MAIL) |

### 12-2. 기능요구사항 — 통합그룹관리자

| ID | 요구사항 |
|---|---|
| FR-TEN-001 | 계열사(테넌트) 신규 등록/비활성화 |
| FR-TEN-002 | 계열사별 사별관리자 지정/해제 |
| FR-TEN-003 | 계열사별 메뉴·기능 활성화 관리 |
| FR-TEN-004 | 그룹 공통 카탈로그/KB 템플릿 작성 및 배포(전 계열사 자동 노출) |
| FR-TEN-005 | (향후) 특정 사용자에게 전사 조직도 열람 권한 부여 |

### 12-3. 화면설계

**SC-TEN-01. 계열사 관리**

```
┌───────────────────────────────────────┐
│ 계열사 목록                 [+ 신규등록]│
├───────────────────────────────────────┤
│ 계열사명   상태    사별관리자   사용자수 │
│ A계열사    활성    김관리자    128       │
│ B계열사    활성    이관리자    64        │
└───────────────────────────────────────┘
```

**SC-ADM-01. 사용자 관리 (사별관리자)** — 이름/이메일/역할/상태 테이블 + 역할변경·비활성화 액션

**SC-ADM-02. 카탈로그 관리** — 항목 목록 + 추가/수정/삭제, (그룹공통 항목은 "그룹배포" 표시, 수정 가능 여부는 설정에 따름)

**SC-ADM-03. 인시던트 카테고리 관리** — 카테고리명 ↔ 담당팀 매핑 테이블 편집

**SC-ADM-04. 결재선/합의자 기본값 설정** — 카탈로그 항목별로 선택적으로 기본 결재선/합의자 지정 (없어도 무방, §CAT-004)

---

## 13. 화면 목록 총괄표

| 화면ID | 화면명 | 대상 사용자 |
|---|---|---|
| SC-AUTH-01~03 | 로그인/회원가입/이메일인증 | 전체 |
| SC-INC-01~03 | 장애신고/티켓상세/내 요청상세 | 일반사용자, 담당자 |
| SC-CAT-01~04 | 카탈로그 목록/요청폼/결재현황/결재처리 | 일반사용자, 결재자·합의자 |
| SC-AST-01~02 | 자산목록/상세 | 사별관리자 |
| SC-KB-01~02 | KB검색/상세작성 | 전체 |
| SC-PORT-01 | 개인 대시보드 | 전체 |
| SC-NOTI-01 | 알림함 | 전체 |
| SC-RPT-01~02 | 통계/전체티켓리스트 | 사별관리자, 통합그룹관리자 |
| SC-ADM-01~05 | 사용자/카탈로그/카테고리/결재설정/메일계정 관리 | 사별관리자 |
| SC-TEN-01~03 | 계열사/사별관리자지정/메뉴관리 | 통합그룹관리자 |

---

## 14. 참고 — 미확정 데이터(개발·온보딩 단계에서 확정)

`itsm_plan2.md` §9 기준, 아래는 화면/기능 구조에는 영향을 주지 않는 설정값이므로 이 문서에서는 UI 훅(설정 화면)만 마련해두고 값 자체는 온보딩 시 입력합니다.

- 카탈로그 초기 항목 목록 (제안 목록은 `itsm_plan2.md` §9 참고)
- 인시던트 카테고리 및 담당팀 매핑표
- 그룹 공통 카탈로그 수정 허용 범위
- 이메일 폴링 주기, 메일 보관 정책
