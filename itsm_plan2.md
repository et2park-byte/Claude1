# ITSM 시스템 구축 방향성 계획서 v2

> `plan.md`에 대한 피드백(주석)을 반영한 개정판입니다. AI 챗봇/에이전트 기능은 이번 단계 범위에서 계속 제외합니다.

## v1 대비 주요 변경사항

- 티켓 생성 경로: 1차는 **웹 포털**이 기본이며, **이메일 자동 티켓화도 사내 POP3 폴링 방식으로 구현하는 것으로 확정** (§3-1-1 신설)
- 인시던트 vs 서비스요청의 "진입 구조" 차이를 명확히 구분
- 댓글/내부메모: 체크박스 방식으로 구체화
- 서비스 카탈로그 항목: 관리자가 추가/삭제 가능하도록 명시
- 승인 워크플로우: 다단계(접수자 → 매니저 승인 → 접수자 처리)로 확정, 매니저 복수 지정 가능
- 그룹웨어: **birtyworks** 사용 중, **OIDC**로 SSO 연동 가능 확인
- 배포 환경: **사내 서버 존재** → 온프레미스 배포로 확정
- 확장 대상 부서: HR/총무/기획팀 등으로 명시적 확정
- 이메일 인프라: **사내에 POP3(수신)/SMTP(발신) 제공 확인** → 이 인프라를 활용해 이메일 티켓화 구현 예정

## 1. 한 줄 방향 정의

사내 IT 지원 업무를 위한 가벼운 ITSM 도구. ITIL 핵심 프로세스만 담아 빠르게 도입하되, 국내 그룹웨어/사내 SSO와 매끄럽게 연동되고, 추후 타 부서(HR/총무/기획팀 등)로 확장 가능한 구조로 설계한다.

## 2. 스코프 정의

| 구분 | 내용 |
|---|---|
| 1차 스코프 (MVP) | IT 헬프데스크 기능만 — 인시던트, 서비스 요청, 자산관리 기본, 지식베이스, 이메일 자동 티켓화(POP3 폴링) |
| 확장 여지 (설계는 지금부터 고려) | 문제관리, 변경관리, SLA 고도화, 타 부서(HR/총무/기획팀 등 ESM) 확장 |
| 아직 안 함 (later) | AI 챗봇/자동화, 고급 리포팅/BI, 모바일 전용 앱 |
| 규모 가정 | 초기엔 소규모(수십~백여 명) 기준으로 설계하되, 사용자/조직 증가 시 구조 변경 없이 스케일 가능해야 함 |

## 3. Phase 1 (MVP) 핵심 기능

### 3-1. 서비스 데스크 / 인시던트 관리

- **티켓 생성 경로**: 웹 포털이 기본 경로. 여기에 더해 **사내 POP3/SMTP 인프라를 활용한 이메일 자동 티켓화도 1차 스코프로 확정** — 상세 방식은 §3-1-1 참고
- **진입 구조 (인시던트 vs 서비스요청 명확히 구분)**:
  - 인시던트(장애 신고)는 **단일 신고 창구** 하나로 운영 — 포털에 "장애 신고" 버튼 하나, 폼 안에서 카테고리(예: 네트워크/계정/하드웨어)를 선택하면 해당 카테고리를 담당하는 팀에게 자동 배정
  - 서비스 요청은 **카탈로그 항목별로 진입점(폼) 자체가 별도** — "팀/카테고리 라우팅" 개념이 아니라 항목 선택 자체가 곧 담당 파악 (§3-2 참고)
- 상태 흐름: 접수 → 처리중 → 대기(사용자 확인 필요) → 해결 → 종료 (커스텀 가능하게)
- 담당자 배정: 인시던트는 카테고리 기반 자동 1차 배정 + 필요 시 수동 재배정
- **댓글/내부 메모**: 댓글 작성 시 기본값은 사용자에게 보이는 일반 답변. "내부 메모" 체크박스를 켜면 담당자에게만 보이는 메모로 전환
- 첨부파일, 스크린샷 첨부
- **우선순위/SLA 타이머**: 우선순위(예: 긴급/보통/낮음)별로 "몇 시간 내 첫 응답, 몇 시간 내 해결"이라는 목표 시간을 설정해두고 시스템이 카운트다운. 기한을 넘기면 경고/에스컬레이션 알림 발생. MVP는 3단계 우선순위 + 단순 응답시간 기준으로 가볍게 시작

### 3-1-1. 이메일 자동 티켓화 (POP3 폴링 방식, 확정)

사내에 **POP3(수신)/SMTP(발신)**가 제공됨을 확인 — 별도 메일 서버나 외부 서비스 없이 기존 인프라로 구현 가능하다고 판단해 1차 스코프에 포함한다.

| 방식 | 설명 | 장단점 |
|---|---|---|
| 기존 메일함 폴링 (POP3) | `it-help@company.com` 같은 전용 계정을 만들고, ITSM 서버가 주기적으로 그 메일함을 POP3로 조회해서 새 메일을 읽어옴 | 별도 인프라 불필요, 기존 사내 메일 시스템(birtyworks 등) 그대로 활용. 다만 폴링 주기만큼 지연 발생, 계정 자격증명을 서버에 저장해야 함 |

**기본 동작 방식**
1. IT지원용 전용 메일 계정(예: `it-help@company.com`)을 하나 생성
2. ITSM 서버가 일정 주기(예: 1~5분)로 해당 계정을 POP3로 폴링해 새 메일 확인
3. 제목 → 티켓 제목, 본문 → 티켓 내용, 발신자 → 요청자로 자동 매핑해 티켓 생성
4. 알림/답장 등 발신 메일은 SMTP로 처리 (§3-6 알림과 연동)

**POP3 특성상 추가로 고려해야 할 점** (IMAP과 달리 서버 쪽에서 읽음 상태/폴더 관리를 지원하지 않음)
- **중복 처리 방지**: 폴링할 때마다 전체 메일함을 다시 읽지 않도록, 이미 처리한 메일을 시스템이 자체적으로 추적해야 함 (예: 메일의 고유 식별자를 저장해두고 비교)
- **서버 보관 정책**: 폴링 후 메일을 서버에서 삭제할지, 남겨둘지 결정 필요 — 삭제하면 유실 위험(장애 시 복구 불가), 남겨두면 사내 메일함 용량이 계속 쌓임 → 일정 기간 후 정리하는 정책 필요
- **스레드 매칭**: 사용자가 알림 메일에 회신하면 새 티켓이 아니라 기존 티켓에 댓글로 붙어야 함 (제목의 티켓번호 또는 메일 헤더로 매칭)
- **스팸/자동회신 루프 방지**: 시스템이 보낸 알림 메일에 부재중 자동응답 등이 회신하면서 무한 루프가 도는 것을 방지하는 로직 필요
- **자격증명 관리**: 전용 계정의 POP3 로그인 정보를 ITSM 서버에 안전하게 저장(환경변수/시크릿 매니저 등)

### 3-2. 서비스 카탈로그 (요청 관리)

- 표준화된 요청 항목 정의 (예: "노트북 신청", "계정 권한 변경", "VPN 발급")
- **항목은 관리자(담당자)가 관리 화면에서 자유롭게 추가/삭제** 가능
- **승인 워크플로우 (다단계로 확정)**:
  1. 요청자가 카탈로그 항목을 요청
  2. 접수자(담당자)가 요청을 접수
  3. 매니저가 업무 처리 여부를 승인/거절 — 매니저는 여러 명 지정 가능하며, 접수자가 건별로 매니저를 지정할 수 있고, 관리자 페이지에서 기본 매니저를 미리 설정해둘 수 있음
  4. 승인되면 접수자(담당자)가 실제 업무를 처리
- 승인/반려 시 알림

### 3-3. 자산관리 (간단한 수준)

- IT 자산 등록(하드웨어/소프트웨어 라이선스) — CMDB까지는 안 가고 대장 수준
- 자산-사용자 매핑 (누가 어떤 장비를 쓰는지)
- 티켓과 자산 연결(어떤 장비에서 발생한 인시던트인지)

### 3-4. 지식베이스 / FAQ

- 카테고리별 문서 작성/검색
- 티켓 해결 시 관련 KB 문서 연결 → 반복 문의 감소

### 3-5. 셀프서비스 포털

- 사용자가 직접 티켓 조회/생성, 카탈로그 요청, KB 검색
- 내 요청 현황 대시보드

### 3-6. 알림

- 이메일 알림 기본 지원
- **사내 그룹웨어 메신저 연동이 가능하면 연동 예정** (티켓 생성/상태변경/댓글/승인요청 시)
- 담당자 SLA 임박 알림

### 3-7. 기본 리포팅

- 티켓 처리 건수, 평균 처리시간, 담당자별 처리량 정도의 기본 대시보드

## 4. 국내 환경 특화 요구사항 (차별화 포인트)

| 항목 | 내용 |
|---|---|
| SSO 연동 | **사내 그룹웨어 SSO 연동 예정.** 현재 사용 중인 그룹웨어는 **birtyworks**이며, **OIDC(OpenID Connect)** 방식으로 연동 가능함을 확인함 |
| 사내 메신저 연동 | 사내 그룹웨어 메신저 연동이 가능하면 알림/티켓 생성 채널로 연동 예정 |
| 그룹웨어 연동 | birtyworks와 조직도/사용자 정보 동기화 — 조직 개편 시 사용자/부서 정보 자동 반영 |
| 조직도 기반 권한 | 부서/직급 기반 승인 라인 반영 → 승인자 지정을 "조직도 상 상위자"로 자동 매핑하는 기능 고려 |
| 한국어 UX | 존댓말 톤, 날짜/시간 형식, 공휴일 캘린더(SLA 계산 시 한국 공휴일 반영) |
| 인증/보안 규제 | 개인정보보호법, 접근이력 로그 요건 등 국내 컴플라이언스 **반드시 고려** |

> birtyworks의 정확한 연동 스펙(OIDC 엔드포인트, 제공 API 범위 등)은 상세 요구사항 단계에서 확인이 필요합니다.

## 5. 확장을 고려한 설계 방향

1. 티켓/카테고리 모델을 IT 전용으로 하드코딩하지 않기 — **HR/총무/기획팀 등에서도 향후 사용 가능하도록** "서비스 유형"을 부서별로 확장 가능한 형태로 설계
2. 승인 워크플로우를 범용화 — IT 승인이든 총무 승인이든 같은 다단계 워크플로우 엔진(접수자→매니저→처리)을 재사용할 수 있게
3. 사용자/조직 정보를 그룹웨어(birtyworks)에서 동기화하는 구조를 처음부터 반영 — 수동 사용자 관리 방식은 나중에 뜯어고쳐야 하므로 지양
4. 권한 체계를 역할(Role) 기반으로 — "IT 담당자"뿐 아니라 나중에 "HR 담당자", "매니저" 등 역할도 쉽게 추가되게

## 6. 비기능 요구사항

- **배포 방식**: 사내 서버가 존재하므로 온프레미스 사내 서버에 배포하는 것으로 확정
- 접근 로그: 티켓/자산 접근 이력 기록 (감사 대응용)
- 백업/복구: 티켓 데이터 정기 백업
- 언어: 한국어 우선, 다국어는 필요 시 나중에

## 7. 다음 단계 (미결정 사항)

그룹웨어(birtyworks/OIDC)와 승인 워크플로우 구조(다단계)는 확정되었습니다. 상세 기능요구서로 넘어가기 전에 아래를 추가로 확정해야 합니다.

1. birtyworks의 정확한 연동 스펙 (OIDC 엔드포인트, 조직도/계정 동기화용 API 제공 여부)
2. 서비스 카탈로그 초기 항목 목록 (관리자가 처음에 등록할 요청 항목들)
3. 인시던트 카테고리 목록 및 카테고리-담당팀 매핑표
4. 매니저 목록 및 기본 매니저 지정 (조직상 누가 매니저 역할을 맡는지)
5. IT지원용 전용 메일 계정 발급 요청 (사내 메일 시스템/IT인프라팀에 문의, POP3/SMTP 접속 정보 확보)
6. 이메일 폴링 주기 및 서버 메일 보관 정책 결정 (몇 분 간격으로 조회할지, 폴링 후 삭제/보관 여부)

## 8. 이번 단계 범위 제외 항목

- AI 챗봇/자동 응답, AI 기반 티켓 자동 분류·처리 등 AI/에이전트 기능 전반 — 추후 별도 검토
