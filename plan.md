# ITSM 시스템 구축 방향성 계획서

> 상세 기능요구서 작성 이전 단계의 방향성 문서입니다. AI 챗봇/에이전트 기능은 이번 단계 범위에서 제외하고, 추후 별도로 검토합니다.

## 1. 한 줄 방향 정의

사내 IT 지원 업무를 위한 가벼운 ITSM 도구. ITIL 핵심 프로세스만 담아 빠르게 도입하되, 국내 그룹웨어/사내 SSO와 매끄럽게 연동되고, 추후 타 부서(HR/총무 등)로 확장 가능한 구조로 설계한다.

## 2. 스코프 정의

| 구분 | 내용 |
|---|---|
| 1차 스코프 (MVP) | IT 헬프데스크 기능만 — 인시던트, 서비스 요청, 자산관리 기본, 지식베이스 |
| 확장 여지 (설계는 지금부터 고려) | 문제관리, 변경관리, SLA 고도화, 타 부서(ESM) 확장 |
| 아직 안 함 (later) | AI 챗봇/자동화, 고급 리포팅/BI, 모바일 전용 앱 |
| 규모 가정 | 초기엔 소규모(수십~백여 명) 기준으로 설계하되, 사용자/조직 증가 시 구조 변경 없이 스케일 가능해야 함 |

규모를 아직 확정하지 못한 상태이므로, "지금 트래픽에 맞춰 최적화"보다는 **나중에 사용자가 늘어나도 구조를 갈아엎지 않아도 되는 선**까지만 확장성을 고려한다 (예: 멀티테넌시까지는 불필요, 조직/부서 단위 데이터 분리는 고려).

## 3. Phase 1 (MVP) 핵심 기능

### 3-1. 서비스 데스크 / 티켓 관리
- 티켓 생성 경로: 웹 포털, 이메일 수신 시 자동 티켓화(선택), 그룹웨어 연동 접수
- 티켓 유형: 인시던트(장애), 서비스 요청(계정발급/장비신청 등) 최소 2종 구분
- 상태 흐름: 접수 → 처리중 → 대기(사용자 확인 필요) → 해결 → 종료 (커스텀 가능하게)
- 담당자 배정: 수동 배정 + 팀/카테고리 기반 자동 라우팅 규칙
- 댓글/내부 메모 구분 (사용자에게 보이는 답변 vs 담당자 간 내부 메모)
- 첨부파일, 스크린샷 첨부
- 티켓 우선순위/SLA 타이머 (기본 SLA 룰 몇 개만)

### 3-2. 서비스 카탈로그 (요청 관리)
- 표준화된 요청 항목 정의 (예: "노트북 신청", "계정 권한 변경", "VPN 발급")
- 요청 항목별 승인 워크플로우 (1단계 승인자 지정 정도로 단순하게 시작)
- 승인/반려 시 알림

### 3-3. 자산관리 (간단한 수준)
- IT 자산 등록(하드웨어/소프트웨어 라이선스) — CMDB까지는 안 가고 대장 수준
- 자산-사용자 매핑 (누가 어떤 장비를 쓰는지)
- 티켓과 자산 연결(어떤 장비에서 발생한 인시던트인지)

### 3-4. 지식베이스 / FAQ
- 카테고리별 문서 작성/검색
- 티켓 해결 시 관련 KB 문서 연결 → 반복 문의 감소

### 3-5. 셀프서비스 포털
- 사용자가 직접 티켓 조회/생성, 카탈로그 요청, KB 검색
- 내 요청 현황 대시보드

### 3-6. 알림
- 이메일 + (연동되면) 사내 메신저 알림: 티켓 생성/상태변경/댓글 시
- 담당자 SLA 임박 알림

### 3-7. 기본 리포팅
- 티켓 처리 건수, 평균 처리시간, 담당자별 처리량 정도의 기본 대시보드

## 4. 국내 환경 특화 요구사항 (차별화 포인트)

| 항목 | 고려사항 |
|---|---|
| SSO 연동 | 사내 인증체계(AD/LDAP, 사내 SSO 서버, 또는 Google Workspace/네이버웍스 등 그룹웨어 계정)와 연동 — 어떤 걸 쓰는지가 설계에 큰 영향을 줌 |
| 사내 메신저 연동 | 카카오워크, 잔디, 슬랙, MS Teams 등 알림/티켓 생성 채널로 연동할지 |
| 그룹웨어 연동 | 네이버웍스, 다우오피스, 그룹웨어(전자결재) 시스템과 조직도/사용자 정보 동기화 — 조직 개편 시 사용자/부서 정보 자동 반영 |
| 조직도 기반 권한 | 한국 기업 특성상 부서/직급 기반 승인 라인이 흔함 → 승인자 지정을 "조직도 상 상위자"로 자동 매핑하는 기능이 유용 |
| 한국어 UX | 존댓말 톤, 날짜/시간 형식, 공휴일 캘린더(SLA 계산 시 한국 공휴일 반영) |
| 인증/보안 규제 | 개인정보보호법, 필요 시 접근이력 로그 요건 등 국내 컴플라이언스 고려 |

"어떤 그룹웨어/SSO를 실제로 쓰는지"가 결정되면 연동 스펙이 좁혀진다. 상세 요구사항 단계에서 반드시 확정해야 할 항목이다.

## 5. 확장을 고려한 설계 방향

1. 티켓/카테고리 모델을 IT 전용으로 하드코딩하지 않기 — 나중에 HR/총무 요청도 같은 테이블 구조로 담을 수 있게, "서비스 유형"을 부서별로 확장 가능한 형태로 설계
2. 승인 워크플로우를 범용화 — IT 승인이든 총무 승인이든 같은 워크플로우 엔진을 재사용할 수 있게
3. 사용자/조직 정보를 그룹웨어에서 동기화하는 구조를 처음부터 — 나중에 수동으로 사용자 관리하는 방식이면 나중에 뜯어고쳐야 함
4. 권한 체계를 역할(Role) 기반으로 — "IT 담당자"뿐 아니라 나중에 "HR 담당자" 역할도 쉽게 추가되게

## 6. 비기능 요구사항 (초안)

- 배포 방식: 사내용이므로 사내 서버 또는 사설 클라우드(VPC) 고려, 외부 SaaS 멀티테넌시는 불필요
- 접근 로그: 티켓/자산 접근 이력 기록 (감사 대응용)
- 백업/복구: 티켓 데이터 정기 백업
- 언어: 한국어 우선, 다국어는 필요 시 나중에

## 7. 다음 단계 (미결정 사항)

상세 기능요구서로 넘어가기 전에 아래를 확정해야 한다.

1. 실제 사용 중인 그룹웨어/SSO 시스템 (연동 스펙 결정)
2. 승인 워크플로우 복잡도 (단순 1단계 승인 vs 다단계 결재)
3. 티켓 카테고리/부서 구조를 실제 회사 조직에 맞게 몇 개로 나눌지

## 8. 이번 단계 범위 제외 항목

- AI 챗봇/자동 응답, AI 기반 티켓 자동 분류·처리 등 AI/에이전트 기능 전반 — 추후 별도 검토
