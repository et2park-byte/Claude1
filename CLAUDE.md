# CLAUDE.md

## 프로젝트 개요

그룹사(계열사) 각각이 독립된 공간으로 사용할 수 있는 온프레미스 멀티테넌트 ITSM 시스템.

- 방향성/의사결정 이력: `plan.md` → `itsm_plan2.md`
- 상세 기능요구서 및 화면설계: `itsm_requirements.md`

## 현재 상태

**아직 코드베이스 없음 — 기획/설계 단계.** 이 문서는 실제 개발을 시작할 때 따를 기술 스택과 핵심 아키텍처 원칙을 미리 정리해둔 것입니다. 개발이 시작되면 실제 디렉터리 구조, 빌드/실행 명령어 등을 이 문서에 추가로 채워 넣어야 합니다.

## 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Java/Kotlin + Spring Boot | OIDC 연동, 배치 작업(Spring Scheduler/Batch) 지원이 성숙함 |
| 프론트엔드 | React + TypeScript | 백엔드와 별도 프로젝트/저장소로 분리 (API 통신) |
| UI 라이브러리 | Ant Design (antd) | `Steps` 컴포넌트로 결재선 진행상황 UI 구현, `Table`/`Form`으로 관리자 화면 구현 |
| DB | PostgreSQL | Row-Level Security(RLS)로 tenant_id 격리를 DB 레벨에서 강제 |
| 인증 | Spring Security OAuth2/OIDC | birtyworks SSO 연동 + 자체가입(이메일 인증) 병행 |
| 배포 | Docker 컨테이너화 | 온프레미스 서버에 배포 |

프론트엔드/백엔드는 별개 프로젝트로 분리 개발하며, 백엔드가 REST API를 제공하고 프론트엔드는 이를 호출해 화면을 그리는 구조입니다.

## 핵심 아키텍처 원칙 (반드시 지킬 것)

1. **테넌트 격리**: 모든 데이터 접근은 `tenant_id` 기준으로 격리되어야 하며, 개별 쿼리마다 산발적으로 필터를 넣는 대신 공통 데이터 접근 계층(가능하면 PostgreSQL RLS)에서 강제해야 함
2. **결재 워크플로우**: 결재선(순차/직렬, 다중 결재자) + 합의자(병렬/동시, 결재자와 동일 권한) 구조. 어느 한쪽이라도 반려하면 전체 요청 반려
3. **관리자 권한 2단계**: 사별관리자(자기 계열사 범위) / 통합그룹관리자(전 계열사 + 계열사 전환 가능)
4. **사용자·조직 데이터**: birtyworks에서 매일 새벽 2시 동기화되는 사내 인사DB를 단일 소스로 사용. birtyworks API 직접 연동은 지양 (SSO 인증 자체는 birtyworks OIDC 사용)
5. **범위 제외**: AI 챗봇/에이전트 기능은 별도 지시가 있기 전까지 구현하지 않음

## 참고 문서

- `plan.md`: 최초 방향성 논의
- `itsm_plan2.md`: 확정된 방향성 v3 (멀티테넌트, 계정관리, 관리자 권한 체계)
- `itsm_requirements.md`: 모듈별 상세 기능요구사항(FR-xxx) 및 화면설계(SC-xxx)
