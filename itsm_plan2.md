# ITSM 시스템 구축 방향성 계획서 v3

> `plan.md` → v2에 이어, **그룹사 멀티테넌트 구조 + 계정관리(자체가입/SSO 병행) + 관리자 권한 체계**를 반영한 개정판입니다. AI 챗봇/에이전트 기능은 이번 단계 범위에서 계속 제외합니다.
>
> 최초 가정했던 항목들(테넌트 판별, 데이터 격리, 자체가입 승인 절차 등)은 피드백을 반영해 **확정 처리**했습니다. 남은 미결정 사항은 §9 참고.

## v2 → v3 주요 변경사항

- 아키텍처를 **멀티테넌트 온프레미스**로 전환 — 그룹사 계열사마다 독립된 공간(데이터/설정)으로 사용, 서버는 물리적으로 하나(또는 소수)의 온프레미스 인프라 공유
- 계정관리: **SSO(birtyworks 단일) + 이메일 인증 기반 자체 회원가입**을 함께 지원, 자체가입은 승인 절차 없이 이메일 인증만으로 즉시 활성화
- 관리자 권한을 2단계로 세분화: **통합그룹관리자**(전사 총괄) / **사별관리자**(계열사별 운영)
- 기존 Phase 1 기능(§6)의 "관리자" 표현을 테넌트 구조에 맞게 "사별관리자" 기준으로 명확화
- **테넌트 판별 방식**: 전 계열사가 동일 이메일 도메인(`@wonik.com`)을 사용해 도메인 기준 자동 판별이 불가함을 확인 → SSO는 birtyworks 클레임으로 자동판별 유력, 자체가입은 이메일 인증 시 자동판별 가능 여부를 먼저 확인하기로 하고 **개발 보류** (§2-4)
- **데이터 격리 방식을 tenant_id 기반으로 확정**하고 동작 원리를 §2-2에 상세 설명 추가

## v3 세부 확정 사항 (검토 라운드 2)

- **모순 수정**: §2-3의 SSO/그룹웨어 연동 정보를 "계열사별 독립"에서 "그룹 공통"으로 정정 (전 계열사 birtyworks 단일 사용 확정과 상충했던 부분)
- 사별관리자·통합그룹관리자에게 **"티켓 전체 리스트 조회"** 권한을 리포팅(통계)과 별개로 명시 (§4, §6-7-1 신규)
- 결재선/합의자 **진행상황 UI**(타임라인/스텝 형태)와 승인 후 소통 방식(기존 댓글 기능 재사용)을 §6-2에 구체화
- 개인 대시보드를 **역할별 탭 구조**(내가 요청한 것 / 내가 처리할 것 / 내가 결재·합의할 것)로 설계 (§6-5)
- 서비스 카탈로그 결재선/합의자 **기본값은 두지 않는 것으로 확정** — 필요 시 사별관리자가 항목별로 직접 설정
- 이메일 자동 티켓화는 **계열사별 전용 메일 계정** 방식으로 확정, 향후 공용 계정 + 자동판별 방식으로도 전환 가능하게 구조를 열어둠
- 계열사(테넌트) 목록은 **통합그룹관리자가 관리 화면에서 직접 등록**하는 방식으로 확정
- (향후) 특정 권한자에게 **전사 조직도 열람 권한**을 부여하는 기능 추가 예정 (§8)

## v3 세부 확정 사항 (검토 라운드 3)

- **자체 회원가입 보류 해제, 확정 처리**: birtyworks API로 매일 새벽 2시 동기화되는 **사내 인사DB**를 활용해, 가입 시 입력한 이메일을 조회해 소속 계열사를 자동 배정하는 방식으로 확정 (§2-4). 신규입사자 등 DB 미반영자는 드롭다운 선택으로 폴백. 퇴사자는 해당 DB에 저장하지 않아 별도 필터링 불필요
- 이 방식 채택으로 **birtyworks의 이메일 조회 API 지원 여부 확인은 더 이상 필요 없음** — §9에서 제거

## 1. 한 줄 방향 정의

**그룹사 계열사들이 각자 독립된 공간으로 사용할 수 있는 온프레미스 멀티테넌트 ITSM 도구.** ITIL 핵심 프로세스만 담아 빠르게 도입하되, 국내 그룹웨어/사내 SSO와 매끄럽게 연동되고, 계열사별 독립 운영 + 그룹 통합관리가 동시에 가능한 구조로 설계한다. 추후 IT 외 부서(HR/총무/기획팀 등)로도 확장 가능해야 한다.

## 2. 아키텍처 방향: 멀티테넌트 온프레미스

### 2-1. 테넌트 개념

- **테넌트 = 계열사 단위.** 각 계열사는 원칙적으로 자기 데이터(사용자, 티켓, 카탈로그, 자산, KB 등)만 보고 다루며 다른 계열사의 데이터는 볼 수 없음. **다만 모든 항목이 계열사 전용은 아니며, 그룹 전체가 공통으로 접근·사용하는 항목도 함께 존재** (§2-3에서 계열사 전용 항목과 그룹 공통 항목을 구분해 정리)
- **배포는 물리적으로 하나(또는 소수)의 온프레미스 서버/인스턴스**에서 이루어지고, 그 안에서 계열사별로 데이터·설정을 논리적으로 분리 — 계열사마다 서버를 따로 세우는 방식이 아님 (관리 부담을 줄이기 위함)

### 2-2. 데이터 격리 전략 (확정: 공유 DB + tenant_id 구분 방식)

**동작 원리**
1. **모든 데이터에 꼬리표를 붙인다**: 사용자, 티켓, 카탈로그 항목, 자산, KB 문서 등 거의 모든 데이터 레코드에 "이건 A계열사 것", "이건 B계열사 것"이라는 `tenant_id` 값을 하나씩 붙여서 저장한다. 물리적으로는 하나의 DB 안에 모든 계열사 데이터가 함께 저장된다.
2. **로그인한 사용자는 자기 tenant_id를 달고 다닌다**: A계열사 직원이 로그인하면 시스템은 "이 사람은 tenant_id=A"라는 것을 세션에 기억해둔다.
3. **모든 조회/저장에 자동으로 필터가 걸린다**: A계열사 직원이 "내 티켓 목록"을 조회하면, 시스템은 항상 `tenant_id = A`인 데이터만 걸러서 보여준다. B계열사의 데이터는 애초에 조회 대상에서 제외되어 화면에 나타나지 않는다.
4. **필터링은 개발자 재량이 아니라 강제되는 공통 장치를 통해야 한다**: 이 필터링을 코드 짤 때마다 개발자가 하나하나 손으로 넣으면, 한 곳이라도 빠뜨리는 순간 다른 계열사 데이터가 새어나오는 사고로 이어진다. 그래서 **모든 데이터 접근이 반드시 거쳐가는 공통 관문(데이터 접근 계층)**을 두고, 그 관문이 tenant_id 필터링을 자동으로 강제하도록 설계한다 — 즉 개발자가 실수로 빠뜨릴 수 없는 구조로 만드는 것이 핵심 원칙.
5. **통합그룹관리자는 예외**: 통합그룹관리자만 관리 화면에서 "지금 어느 계열사를 보고 있는지" 전환할 수 있어 여러 tenant_id를 넘나들며 조회 가능. 일반 사용자·사별관리자는 자기 tenant_id 밖으로 나갈 수 없음.

정리하면, **물리적으로는 한 곳에 섞여 저장되지만, tenant_id라는 꼬리표와 그걸 강제하는 공통 필터링 장치 덕분에 논리적으로는 완전히 분리된 것처럼 동작**한다.

| 방식 | 설명 | 채택 여부 |
|---|---|---|
| 공유 DB + tenant_id 컬럼 구분 | 위 방식 | **확정** — 온프레미스 환경이라 물리적 보안은 이미 확보되어 있고, 관리 단순화에 유리 |
| 계열사별 완전히 별도 DB | 계열사마다 DB를 통째로 분리 | 채택 안 함 |

> ⚠️ 공유 DB 방식을 택했으므로 **모든 데이터 조회/저장 로직에 tenant_id 필터링이 빠짐없이 적용되어야** 다른 계열사 데이터 노출 사고를 막을 수 있습니다. 이건 상세설계 단계에서 반드시 원칙으로 못박아야 하는 부분입니다.

### 2-3. 테넌트별로 분리되어야 하는 것 / 그룹 공통으로 둘 수 있는 것

| 구분 | 계열사별로 독립 | 그룹 공통 (통합그룹관리자가 관리) |
|---|---|---|
| 사용자·조직도 | ✅ 계열사 소속 사용자만 조회 가능 | (향후) 특정 권한자에게는 **전사 조직도 열람 권한**을 별도로 부여하는 기능 추가 예정 — §8 확장 항목 참고 |
| 서비스 카탈로그 항목 | ✅ 계열사 자율 항목 추가 가능 | 통합그룹관리자가 만든 공통 템플릿을 "배포"하면 각 계열사 카탈로그에 자동으로 노출됨 — 계열사가 그대로 쓰거나 커스터마이징 (§9 참고) |
| 인시던트 카테고리/담당팀 매핑 | ✅ | — |
| 결재선/합의자 설정 | ✅ | — |
| 자산 목록 | ✅ | — |
| 지식베이스(KB) | ✅ 계열사별 문서 | 공통 KB(예: 전사 보안정책 안내)는 그룹관리자가 전체 배포 가능 |
| SSO/그룹웨어 연동 정보 | — | ✅ **전 계열사가 birtyworks를 공통 사용(확정)하므로, SSO 연동 설정(엔드포인트·클라이언트 정보 등)은 계열사별이 아니라 그룹 공통 설정 하나로 구성** |
| 플랫폼 기능/메뉴 활성화 여부 | — | ✅ 통합그룹관리자가 계열사별로 어떤 메뉴/기능을 켤지 결정 |

> ⚠️ **모순 수정**: 이전 버전에서 "SSO/그룹웨어 연동 정보"를 계열사별 독립 항목으로 뒀었는데, 이는 계열사마다 다른 그룹웨어를 쓸 가능성을 가정했을 때의 문구가 "전 계열사 birtyworks 공통 사용" 확정 이후에도 남아있던 것입니다. 그룹 공통 항목으로 정정했습니다 (§8의 관련 문구도 함께 수정).

### 2-4. 테넌트 판별 방식 (확정: SSO는 클레임, 자체가입은 사내 인사DB 조회)

**모든 계열사가 동일한 이메일 도메인(`@wonik.com`)을 사용하므로 도메인 기준 자동 판별은 불가능함을 확인.** 대신 아래 두 경로로 각각 자동 판별한다.

| 경로 | 방식 | 상태 |
|---|---|---|
| **SSO 로그인** | birtyworks가 제공하는 **회사코드/사업장코드 클레임**으로 자동 판별 | 유력한 방법 — birtyworks 연동 스펙 확인 시 이 클레임 제공 여부를 반드시 확인 (§9) |
| **자체 회원가입** | **사내 인사정보 DB 조회 방식으로 확정.** birtyworks API를 통해 매일 새벽 2시 인사정보를 동기화해두는 내부 DB가 이미 존재 — 가입 시 입력한 이메일을 이 DB에서 조회해 일치하는 소속 계열사를 자동으로 가져옴 | **확정** |

**이 방식을 채택한 이유**: birtyworks에 별도의 실시간 조회 API가 있는지 여부와 무관하게, 이미 매일 동기화되는 내부 인사DB를 활용하면 즉시 구현 가능하고 birtyworks 가용성에도 의존하지 않음. 업계에서도 실시간 연동(SCIM 등) 대신 **배치 동기화 + 로컬 조회** 방식은 흔히 쓰이는 패턴.

**설계 세부사항**
1. **처리 흐름**: ① 사용자가 가입 화면에서 이메일 입력 → ② 인증메일 발송 **전에** 먼저 이 이메일로 인사DB 조회 → ③ 일치하는 소속이 있으면 인증메일 발송, **없으면 즉시 안내하고 계열사 드롭다운 선택으로 폴백** → ④ 인증 완료 시점에 조회해둔 소속 계열사로 자동 배정하며 계정 활성화
2. **신규 입사자 등 DB 미반영자 대응**: 배치가 하루 1회(새벽 2시)만 도니, 당일 입사자 등은 아직 DB에 없을 수 있음 → 이 경우 위 흐름의 ③번처럼 드롭다운 선택으로 폴백 (완전히 막지 않음)
3. **퇴사자 필터링 불필요**: 이 인사DB에는 퇴사자 정보를 저장하지 않으므로 별도 필터링 로직 불필요 (확인됨)
4. **이메일 정규화**: 조회 전 대소문자 통일·공백 제거 등 정규화 처리
5. **DB 접근 통제**: ITSM 서버는 이 인사DB에 **"이메일로 소속 계열사만 조회"하는 최소 권한**으로 접근 — 다른 인사정보(연봉, 인사평가 등)는 조회 범위에 포함하지 않음
6. **배치 실패 감지**: 새벽 2시 동기화 배치의 성공/실패 여부를 모니터링해, 실패 시 그날은 조회 결과가 오래된 데이터일 수 있음을 인지

## 3. 계정관리 및 인증

### 3-1. 로그인 방식 (SSO + 자체 회원가입 병행)

- **SSO 연동**: 전 계열사가 **birtyworks** 그룹웨어 계정으로 로그인 (확정 — 계열사별로 다른 그룹웨어를 쓰는 곳은 없음). 로그인 시 회사코드 클레임이 있다면 테넌트도 자동 판별 (§2-4)
- **자체 회원가입 (이메일 인증) — 확정, 개발 진행**: SSO 연동 전이거나 그룹웨어 계정이 없는 사용자를 위해 병행 지원
  1. 이름/사내 이메일/비밀번호로 가입 신청
  2. 인증메일 발송 **전에** 입력한 이메일을 사내 인사DB(매일 새벽 2시 birtyworks 동기화)에서 조회해 소속 계열사 자동 확인 — 조회 안 되면 계열사 드롭다운 선택으로 폴백 (§2-4)
  3. 이메일 인증 메일 발송 → 인증 완료 시 **계정 즉시 활성화**하며 앞서 조회한 소속 계열사로 자동 배정 (별도 승인 절차 없음, §3-2)
- 계열사별로 "SSO만 허용" / "자체가입만 허용" / "둘 다 허용" 중 선택할 수 있게 설정 가능하게 설계 (계열사마다 SSO 연동 준비 상태가 다를 수 있으므로)

### 3-2. 가입 후 권한 부여 (확정: 승인 절차 없이 즉시 활성화)

- 자체 회원가입으로 들어온 신규 계정은 **이메일 인증만으로 즉시 활성화**, 별도의 사별관리자 승인 절차는 두지 않음 — 확정
- 활성화 시 기본적으로 "일반 사용자" 권한으로 생성되며, 결재자/사별관리자 등 별도 역할은 이후 사별관리자가 필요에 따라 부여
- 인사DB 조회로 자동 배정되지 못해 드롭다운으로 직접 선택한 경우에 대비해, 사별관리자가 신규 가입자 목록에서 소속 계열사를 확인/정정할 수 있는 기능은 열어둔다

## 4. 관리자 권한 체계 (2단계)

| 레벨 | 역할 | 권한 범위 |
|---|---|---|
| **통합그룹관리자** | 그룹 전체 총괄 | - 계열사(테넌트) 신규 추가/삭제<br>- 각 계열사의 사별관리자 지정/해제<br>- 전체 메뉴·기능 활성화 관리 (계열사별로 어떤 모듈을 켤지)<br>- 그룹 공통 카탈로그 템플릿/공통 정책 배포<br>- **전 계열사 통합 티켓 리스트 조회** (모든 계열사의 티켓을 계열사 구분 없이 또는 계열사별 필터로 조회, 개별 티켓 상세 내용 열람 가능)<br>- 전체 계열사 통합 리포팅(통계) 열람 |
| **사별관리자** | 계열사별 운영 담당 | - 자기 계열사 사용자 계정 관리 (역할 부여/비활성화/소속 오선택 정정)<br>- 자기 계열사 서비스 카탈로그 항목 추가/삭제 (§6-2)<br>- 자기 계열사 인시던트 카테고리·담당팀 매핑 관리<br>- 자기 계열사 결재선/합의자 기본값 지정 (§6-2)<br>- **자기 계열사 전체 티켓 리스트 조회** (담당자 배정 여부와 무관하게 진행중/완료 티켓 전체를 목록으로 보고, 개별 티켓 상세 내용까지 열람)<br>- 자기 계열사 리포팅(통계) 열람 |
| **결재자/합의자** | 승인 프로세스 참여자 | 담당 요청 건에 대한 승인/반려 (§6-2) |
| **일반 사용자** | 전 직원 | 티켓 생성/조회, 카탈로그 요청, KB 검색 |

**권한 관계**: 통합그룹관리자는 모든 계열사를 관리 화면에서 전환하며 볼 수 있지만, 사별관리자는 자기 소속 계열사 범위 밖의 데이터는 접근할 수 없음 (§2-2 데이터 격리와 직결).

> **"리포팅"과 "티켓 리스트 조회"는 서로 다른 기능**입니다. 리포팅은 처리 건수·평균 처리시간 같은 **통계 대시보드**(§6-7)이고, 티켓 리스트 조회는 **개별 티켓을 하나하나 목록으로 보고 클릭해서 상세 내용까지 볼 수 있는 화면**(§6-7-1, 신규)입니다. 둘 다 사별관리자/통합그룹관리자에게 필요합니다.

> 이전 버전(v2)에서 "관리자(담당자)가 카탈로그 항목 추가/삭제" 등으로 표현했던 부분은 이제 **"사별관리자"**로 명확히 합니다.

## 5. 스코프 정의

| 구분 | 내용 |
|---|---|
| 1차 스코프 (MVP) | 멀티테넌트 기반 위에서 — 인시던트, 서비스 요청, 자산관리 기본, 지식베이스, 이메일 자동 티켓화(POP3 폴링), 2단계 관리자 권한 |
| 확장 여지 (설계는 지금부터 고려) | 문제관리, 변경관리, SLA 고도화, 타 부서(HR/총무/기획팀 등 ESM) 확장, 그룹 통합 리포팅 고도화 |
| 아직 안 함 (later) | AI 챗봇/자동화, 고급 리포팅/BI, 모바일 전용 앱 |
| 규모 가정 | 계열사 수 및 계열사별 사용자 규모 미정 — 구조 변경 없이 계열사 추가 가능해야 함 |

## 6. Phase 1 (MVP) 핵심 기능

### 6-1. 서비스 데스크 / 인시던트 관리

- **티켓 생성 경로**: 웹 포털이 기본 경로. 여기에 더해 **사내 POP3/SMTP 인프라를 활용한 이메일 자동 티켓화도 1차 스코프로 확정** — 상세 방식은 §6-1-1 참고. **계열사별로 전용 메일 계정을 각각 두는 방식으로 확정** (§6-1-1)
- **진입 구조 (인시던트 vs 서비스요청 명확히 구분)**:
  - 인시던트(장애 신고)는 **단일 신고 창구** 하나로 운영 — 포털에 "장애 신고" 버튼 하나, 폼 안에서 카테고리(예: 네트워크/계정/하드웨어)를 선택하면 해당 계열사 내 카테고리 담당팀에게 자동 배정
  - 서비스 요청은 **카탈로그 항목별로 진입점(폼) 자체가 별도** — "팀/카테고리 라우팅" 개념이 아니라 항목 선택 자체가 곧 담당 파악 (§6-2 참고)
- 상태 흐름: 접수 → 처리중 → 대기(사용자 확인 필요) → 해결 → 종료 (커스텀 가능하게)
- 담당자 배정: 인시던트는 카테고리 기반 자동 1차 배정 + 필요 시 수동 재배정 (모두 소속 계열사 범위 내에서만)
- **댓글/내부 메모**: 댓글 작성 시 기본값은 사용자에게 보이는 일반 답변. "내부 메모" 체크박스를 켜면 담당자에게만 보이는 메모로 전환
- 첨부파일, 스크린샷 첨부
- **우선순위/SLA 타이머**: 우선순위(예: 긴급/보통/낮음)별로 "몇 시간 내 첫 응답, 몇 시간 내 해결"이라는 목표 시간을 설정해두고 시스템이 카운트다운. 기한을 넘기면 경고/에스컬레이션 알림 발생. MVP는 3단계 우선순위 + 단순 응답시간 기준으로 가볍게 시작

### 6-1-1. 이메일 자동 티켓화 (POP3 폴링 방식, 확정)

사내에 **POP3(수신)/SMTP(발신)**가 제공됨을 확인 — 별도 메일 서버나 외부 서비스 없이 기존 인프라로 구현 가능하다고 판단해 1차 스코프에 포함한다.

| 방식 | 설명 | 장단점 |
|---|---|---|
| 기존 메일함 폴링 (POP3) | `it-help@company.com` 같은 전용 계정을 만들고, ITSM 서버가 주기적으로 그 메일함을 POP3로 조회해서 새 메일을 읽어옴 | 별도 인프라 불필요, 기존 사내 메일 시스템(birtyworks 등) 그대로 활용. 다만 폴링 주기만큼 지연 발생, 계정 자격증명을 서버에 저장해야 함 |

**기본 동작 방식**
1. IT지원용 전용 메일 계정(예: `it-help@company.com`)을 하나 생성
2. ITSM 서버가 일정 주기(예: 1~5분)로 해당 계정을 POP3로 폴링해 새 메일 확인
3. 제목 → 티켓 제목, 본문 → 티켓 내용, 발신자 → 요청자로 자동 매핑해 티켓 생성
4. 알림/답장 등 발신 메일은 SMTP로 처리 (§6-6 알림과 연동)

**POP3 특성상 추가로 고려해야 할 점** (IMAP과 달리 서버 쪽에서 읽음 상태/폴더 관리를 지원하지 않음)
- **중복 처리 방지**: 폴링할 때마다 전체 메일함을 다시 읽지 않도록, 이미 처리한 메일을 시스템이 자체적으로 추적해야 함 (예: 메일의 고유 식별자를 저장해두고 비교)
- **서버 보관 정책**: 폴링 후 메일을 서버에서 삭제할지, 남겨둘지 결정 필요 — 삭제하면 유실 위험(장애 시 복구 불가), 남겨두면 사내 메일함 용량이 계속 쌓임 → 일정 기간 후 정리하는 정책 필요
- **스레드 매칭**: 사용자가 알림 메일에 회신하면 새 티켓이 아니라 기존 티켓에 댓글로 붙어야 함 (제목의 티켓번호 또는 메일 헤더로 매칭)
- **스팸/자동회신 루프 방지**: 시스템이 보낸 알림 메일에 부재중 자동응답 등이 회신하면서 무한 루프가 도는 것을 방지하는 로직 필요
- **자격증명 관리**: 전용 계정의 POP3 로그인 정보를 ITSM 서버에 안전하게 저장(환경변수/시크릿 매니저 등)
- **멀티테넌트 방식 (확정)**: 계열사별로 전용 메일 계정을 각각 둔다 (예: `it-help-A@wonik.com`, `it-help-B@wonik.com`). 각 계정에 미리 tenant_id를 매핑해두면, 어느 계정으로 온 메일인지에 따라 자동으로 해당 계열사 티켓으로 생성됨
  - **향후 확장 여지**: 나중에 공용 계정 하나로 받고 발신자 정보(사번, 조직 코드 등)로 자동 테넌트 판별하는 방식으로도 전환 가능하도록, 메일 파싱 로직은 "어느 계정으로 왔는지"뿐 아니라 "발신자 기반으로도 tenant_id를 판별할 수 있는" 구조로 열어두고 설계

### 6-2. 서비스 카탈로그 (요청 관리)

- 표준화된 요청 항목 정의 (예: "노트북 신청", "계정 권한 변경", "VPN 발급")
- **항목은 사별관리자가 관리 화면에서 자유롭게 추가/삭제** 가능 (그룹 공통 템플릿이 있다면 통합그룹관리자가 배포, §2-3)
- **초기 카탈로그 항목**: 시스템 도입 시 ITSM에서 흔히 쓰이는 항목 몇 개를 기본으로 제공하고, 나머지는 사별관리자가 필요에 따라 추가 (제안 목록은 §9 참고)
- **승인 워크플로우 (다단계 + 결재선/합의자 구조로 확정)**:
  1. 요청자가 카탈로그 항목을 요청
  2. 접수자(담당자)가 요청을 접수하고, 건별로 **결재선**과 **합의자**를 지정 — **항목별 기본 결재선/합의자는 두지 않음(확정)**, 필요하면 사별관리자가 항목별로 직접 설정 가능
  3. **결재 단계** — 아래 두 그룹이 동시에 진행됨
     - **결재선 (순차/직렬)**: 결재자를 여러 명 지정 가능하며, 지정된 순서대로 1차 결재자 승인 → 2차 결재자 승인 → ... 순으로 진행
     - **합의자 (병렬/동시)**: 결재선이 진행되는 동안 **동시에** 검토하며, 결재자와 **동일한 승인/반려 권한**을 가짐
     - 결재선의 결재자 중 한 명이라도 반려하거나, 합의자 중 한 명이라도 반려하면 **전체 요청이 반려 처리**됨
     - 결재선 전원의 순차 승인 + 합의자 전원의 승인이 모두 완료되어야 최종 승인
  4. 승인되면 접수자(담당자)가 실제 업무를 처리
- 승인/반려 시 알림 (결재선의 각 순번, 합의자 각각에게 개별적으로 승인 요청 알림 발송)
- 결재자/합의자는 **같은 계열사 소속 인원 중에서만** 지정 가능 (계열사 간 결재 교차는 1차 스코프에서 지원하지 않음)
- **결재 진행상황 UI**: 결재선/합의자의 승인 상태를 **타임라인/스텝 형태**로 표시 — 예: "1차 결재자(승인완료) → 2차 결재자(대기중)"처럼 순서를 보여주고, 합의자는 별도 트랙으로 "합의자 A(승인완료), 합의자 B(대기중)"처럼 병렬 표시. 요청자·접수자·결재자 모두 이 진행 화면을 조회 가능
- **승인 후 요청자-담당자 소통**: 별도 메시지 시스템을 새로 만들지 않고, 승인된 요청은 사실상 하나의 처리 티켓이 되므로 **§6-1에서 설계한 댓글(사용자에게 보이는 답변) / 내부메모(담당자만) 구조를 그대로 재사용**

### 6-3. 자산관리 (간단한 수준)

- IT 자산 등록(하드웨어/소프트웨어 라이선스) — CMDB까지는 안 가고 대장 수준
- 자산-사용자 매핑 (누가 어떤 장비를 쓰는지), 계열사별로 자산 목록 분리
- 티켓과 자산 연결(어떤 장비에서 발생한 인시던트인지)

### 6-4. 지식베이스 / FAQ

- 카테고리별 문서 작성/검색 (계열사별 KB + 그룹 공통 KB 병행, §2-3)
- 티켓 해결 시 관련 KB 문서 연결 → 반복 문의 감소

### 6-5. 셀프서비스 포털

- 사용자가 직접 티켓 조회/생성, 카탈로그 요청, KB 검색
- 로그인 시 자신이 속한 계열사 화면으로 자동 진입 (테넌트 전환 없음 — 일반 사용자는 자기 계열사만 봄)
- **개인 대시보드 — 역할별 탭 구조 (확정)**: 역할은 사람에게 고정되지 않고 **"이 티켓에서 어떤 역할인지"로 건별로 결정**됨 (같은 사람이 어떤 티켓에서는 요청자, 다른 티켓에서는 담당자·결재자일 수 있음). 이를 반영해 개인 대시보드를 역할별 탭으로 구성:
  - **내가 요청한 것** — 내가 생성한 티켓/카탈로그 요청 현황
  - **내가 처리할 것** — 담당자로 배정된 티켓 목록
  - **내가 결재·합의할 것** — 결재자/합의자로 지정된 승인 대기 건
  - 로그인한 사용자는 이 통합 대시보드 하나에서 자신의 모든 역할을 확인

### 6-6. 알림

- 이메일 알림 기본 지원
- **사내 그룹웨어 메신저 연동이 가능하면 연동 예정** (티켓 생성/상태변경/댓글/승인요청 시)
- 담당자 SLA 임박 알림

### 6-7. 기본 리포팅

- 계열사별 티켓 처리 건수, 평균 처리시간, 담당자별 처리량 **통계** 대시보드 (사별관리자용)
- 전체 계열사 통합 통계 대시보드 (통합그룹관리자용)

### 6-7-1. 전체 티켓 리스트 조회 (신규)

리포팅(통계)과는 별개로, **개별 티켓을 하나하나 목록으로 보고 클릭해서 상세 내용까지 조회할 수 있는 화면**이 필요하다 (§4).

- **사별관리자**: 자기 계열사의 전체 티켓을 리스트로 조회 — 담당자 배정 여부와 무관하게 진행중/완료 티켓 모두 포함, 개별 티켓 클릭 시 상세 내용(댓글, 처리 이력 등)까지 열람 가능
- **통합그룹관리자**: 전 계열사의 티켓을 통합해서 리스트로 조회 — 계열사 필터로 특정 계열사만 보거나, 전체를 한 번에 조회 가능
- 상태/카테고리/담당자/기간 등으로 필터링 가능하게 설계

## 7. 국내 환경 특화 요구사항 (차별화 포인트)

| 항목 | 내용 |
|---|---|
| SSO 연동 | **전 계열사가 birtyworks 그룹웨어를 공통으로 사용 (확정).** **OIDC(OpenID Connect)** 방식으로 연동 가능함을 확인함. 계열사별로 다른 그룹웨어를 쓰는 곳은 없어 인증 연동 자체는 단일화 가능 (§3-1) |
| 자체 회원가입 | SSO 연동 전이거나 그룹웨어 계정이 없는 사용자를 위한 이메일 인증 기반 가입 병행. **사내 인사DB(매일 새벽 2시 birtyworks 동기화) 조회로 소속 계열사 자동 배정, 이메일 인증만으로 즉시 활성화** — 확정 (§2-4, §3-1~3-2) |
| 사내 메신저 연동 | 사내 그룹웨어 메신저 연동이 가능하면 알림/티켓 생성 채널로 연동 예정 |
| 그룹웨어 연동 | birtyworks와 조직도/사용자 정보 동기화 — 조직 개편 시 사용자/부서 정보 자동 반영 |
| 조직도 기반 권한 | 부서/직급 기반 승인 라인 반영 → 승인자 지정을 "조직도 상 상위자"로 자동 매핑하는 기능 고려 |
| 한국어 UX | 존댓말 톤, 날짜/시간 형식, 공휴일 캘린더(SLA 계산 시 한국 공휴일 반영) |
| 인증/보안 규제 | 개인정보보호법, 접근이력 로그 요건 등 국내 컴플라이언스 **반드시 고려** — 계열사 간 데이터 격리도 이 컴플라이언스의 일부 |

## 8. 확장을 고려한 설계 방향

1. 티켓/카테고리 모델을 IT 전용으로 하드코딩하지 않기 — **HR/총무/기획팀 등에서도 향후 사용 가능하도록** "서비스 유형"을 부서별로 확장 가능한 형태로 설계
2. 승인 워크플로우를 범용화 — IT 승인이든 총무 승인이든 같은 워크플로우 엔진(접수자→결재선/합의자→처리)을 재사용할 수 있게. 결재선/합의자 구조는 향후 법무/재무 등으로 확장 시에도 그대로 재사용 가능해야 함
3. 사용자/조직 정보를 그룹웨어(birtyworks, 그룹 공통 연동)에서 동기화하는 구조를 처음부터 반영 — 수동 사용자 관리 방식은 나중에 뜯어고쳐야 하므로 지양
4. 권한 체계를 역할(Role) 기반으로 — "IT 담당자"뿐 아니라 "HR 담당자", "매니저", "통합그룹관리자" 등 역할도 쉽게 추가되게
5. **신규 계열사(테넌트) 추가가 코드 변경 없이 데이터 등록만으로 가능해야 함** — 계열사가 늘어날 때마다 개발 작업이 필요하면 멀티테넌트 설계의 의미가 없음. 등록은 **통합그룹관리자가 관리 화면에서 직접 등록**하는 방식으로 확정
6. **(향후) 전사 조직도 열람 권한**: 특정 권한을 부여받은 사용자는 자기 계열사를 넘어 전사(그룹 전체) 조직도를 열람할 수 있는 기능을 추후 추가 — 기본은 계열사 내부만 보이는 것이 원칙이므로 예외 권한으로 설계 (§2-3)

## 9. 다음 단계 (미결정 사항)

**이번 라운드에서 새로 확정된 것**: 계열사(테넌트) 목록은 통합그룹관리자가 직접 등록, 자체가입은 이메일 인증 시 계열사 자동판별 가능 여부 확인 전까지 개발 보류, 그룹 공통 카탈로그 항목 "배포"의 의미 정리, 이메일 티켓화는 계열사별 전용 계정 방식으로 확정(향후 공용계정 자동판별로 확장 가능하게 설계), 카탈로그 초기 항목은 공통 기본 세트 제공 후 자율 추가, 결재선/합의자 기본값은 두지 않고 필요시 사별관리자가 설정, 사별관리자/통합그룹관리자에게 티켓 전체 리스트 조회 권한 추가.

### 여전히 확인/결정이 필요한 항목 (blocking)

1. **birtyworks의 정확한 연동 스펙** — 자체가입 쪽은 사내 인사DB 조회로 해결되어 더 이상 필요 없음. 남은 확인 항목:
   - OIDC 로그인 응답(클레임)에 회사코드/사업장코드 같은 계열사 식별 정보가 포함되는지 (§2-4, SSO 자동판별용)
   - 조직도/계정 동기화용 API 제공 여부 (§8)
2. **그룹 공통 카탈로그/KB의 범위**: 통합그룹관리자가 배포(=자동으로 각 계열사 카탈로그에 노출)하는 공통 항목을, 계열사가 자기 사정에 맞게 수정할 수 있게 할지 아니면 그대로 강제 적용할지
3. **서비스 카탈로그 초기 항목 목록 (제안, 확인 필요)**: 아래처럼 ITSM에서 흔히 쓰이는 항목을 기본 세트로 제안 — 검토 후 추가/삭제해주시면 반영
   - 신규 PC/노트북 지급 신청
   - 소프트웨어 설치 · 라이선스 신청
   - 계정 생성 및 권한 변경
   - VPN 접속 신청
   - 비밀번호 초기화
   - 이메일 계정 신청
   - 모바일 기기(업무폰) 신청
4. **인시던트 카테고리 목록 및 카테고리-담당팀 매핑표 (계열사별)**: "네트워크 문제는 네트워크팀, 계정 문제는 인프라팀"처럼 카테고리와 담당팀을 미리 짝지어야 신고 시 자동 배정이 가능함 — 계열사별로 실제 조직에 맞는 카테고리/담당팀 목록 정리 필요

### 개발 단계에서 함께 정하기로 한 항목 (지금은 보류)

5. IT지원용 전용 메일 계정 발급 요청 (사내 메일 시스템/IT인프라팀에 문의, POP3/SMTP 접속 정보 확보)
6. 이메일 폴링(서버가 주기적으로 새 메일이 왔는지 스스로 확인하러 가는 방식) 주기 및 서버 메일 보관 정책 결정

## 10. 이번 단계 범위 제외 항목

- AI 챗봇/자동 응답, AI 기반 티켓 자동 분류·처리 등 AI/에이전트 기능 전반 — 추후 별도 검토
- 계열사 간 결재 교차(한 계열사 요청을 다른 계열사 인원이 결재) — 1차 스코프에서는 지원하지 않음
