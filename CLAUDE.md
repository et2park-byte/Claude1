# CLAUDE.md

## 프로젝트 개요

그룹사(계열사) 각각이 독립된 공간으로 사용할 수 있는 온프레미스 멀티테넌트 ITSM 시스템.

- 방향성/의사결정 이력: `plan.md` → `itsm_plan2.md`
- 상세 기능요구서 및 화면설계: `itsm_requirements.md`

## 현재 상태

**아직 코드베이스 없음 — 기획/설계 단계.** 이 문서는 실제 개발을 시작할 때 따를 기술 스택과 핵심 아키텍처 원칙을 미리 정리해둔 것입니다. 개발이 시작되면 실제 디렉터리 구조, 빌드/실행 명령어 등을 이 문서에 추가로 채워 넣어야 합니다.

## 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Java/Kotlin + Spring Boot | OIDC 연동, 배치 작업(Spring Scheduler/Batch) 지원이 성숙함 |
| 프론트엔드 | React + TypeScript | 백엔드와 별도 프로젝트/저장소로 분리 (API 통신) |
| UI 라이브러리 | Ant Design (antd) | `Steps` 컴포넌트로 결재선 진행상황 UI 구현, `Table`/`Form`으로 관리자 화면 구현 |
| DB | PostgreSQL | Row-Level Security(RLS)로 tenant_id 격리를 DB 레벨에서 강제 |
| 인증 | Spring Security OAuth2/OIDC | birtyworks SSO 연동 + 자체가입(이메일 인증) 병행 |
| 서버 OS | **Windows Server** | 사내 서버가 Windows 환경 — Docker는 사용하지 않음 |
| 배포 | **Spring Boot를 Windows Service로 등록 + IIS 리버스프록시** | 아래 "배포 아키텍처" 참고 |

프론트엔드/백엔드는 별개 프로젝트로 분리 개발하며, 백엔드가 REST API를 제공하고 프론트엔드는 이를 호출해 화면을 그리는 구조입니다.

## 배포 아키텍처 (Windows Server)

```
[사용자 브라우저]
        │ HTTPS (443)
        ▼
   IIS (윈도우 서버, 80/443)
   - 정적파일(React 빌드 결과물) 직접 서빙
   - /api/* 요청은 리버스프록시로 아래에 전달
        │ (내부, 예: localhost:8080)
        ▼
   Spring Boot (Windows Service로 등록, 내장 톰캣)
        ▼
   PostgreSQL
```

- **Spring Boot 서비스화**: JAR을 그냥 실행해두면 재부팅/장애 시 안 살아나므로, **WinSW** 또는 **NSSM** 중 하나로 Windows Service에 등록한다 (부팅 시 자동 시작, 크래시 시 자동 재시작)
- **IIS 리버스프록시**: 기본 IIS에는 리버스프록시 기능이 없으므로 **URL Rewrite**, **Application Request Routing(ARR)** 모듈을 추가 설치해야 한다. `/api/*` 요청을 Spring Boot 포트(예: 8080)로 프록시하는 규칙을 설정한다
- **정적 프론트엔드**: React 빌드 결과물은 IIS 사이트 폴더에 배치해 IIS가 직접 서빙한다 (별도 Node 서버 불필요)
- **배치 작업**: POP3 폴링, 인사DB 동기화 반영 등은 별도 Windows 작업 스케줄러 없이 Spring Boot 내장 스케줄러(`@Scheduled`)로 처리 — 서비스가 떠 있는 동안 자동 실행됨

## 핵심 아키텍처 원칙 (반드시 지킬 것)

1. **테넌트 격리**: 모든 데이터 접근은 `tenant_id` 기준으로 격리되어야 하며, 개별 쿼리마다 산발적으로 필터를 넣는 대신 공통 데이터 접근 계층(가능하면 PostgreSQL RLS)에서 강제해야 함
2. **결재 워크플로우**: 결재선(순차/직렬, 다중 결재자) + 합의자(병렬/동시, 결재자와 동일 권한) 구조. 어느 한쪽이라도 반려하면 전체 요청 반려
3. **관리자 권한 2단계**: 사별관리자(자기 계열사 범위) / 통합그룹관리자(전 계열사 + 계열사 전환 가능)
4. **사용자·조직 데이터**: birtyworks에서 매일 새벽 2시 동기화되는 사내 인사DB를 단일 소스로 사용. birtyworks API 직접 연동은 지양 (SSO 인증 자체는 birtyworks OIDC 사용)
5. **범위 제외**: AI 챗봇/에이전트 기능은 별도 지시가 있기 전까지 구현하지 않음
6. **목록/리포팅 화면은 서버사이드 페이지네이션 필수**: 전체 티켓 리스트, 통계 대시보드, 사용자 관리 등 목록형 화면은 필터가 없는 상태에서도 전체 데이터를 한 번에 내려주지 않는다 (사용자 4,000~6,000명 규모 대비, `itsm_requirements.md` FR-COM-006)
7. **알림 발송은 비동기 처리**: 이메일/메신저 알림은 큐에 넣어 비동기로 발송하고, 사용자 액션에 대한 응답이 알림 발송 완료를 기다리지 않게 한다 (FR-COM-007, FR-NOTI-005)
8. **첨부파일 저장 경로는 설정값으로 관리**: 초기에는 서버 로컬 디스크에 `tenant_id`별 폴더로 저장하되, 경로를 하드코딩하지 않아 추후 별도 파일서버로 이전 시 코드 변경 없이 대응 가능하게 함 (FR-COM-008)

## 참고 문서

- `plan.md`: 최초 방향성 논의
- `itsm_plan2.md`: 확정된 방향성 v3 (멀티테넌트, 계정관리, 관리자 권한 체계)
- `itsm_requirements.md`: 모듈별 상세 기능요구사항(FR-xxx) 및 화면설계(SC-xxx)
